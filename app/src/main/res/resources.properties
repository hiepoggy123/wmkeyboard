# Declares the language of the unqualified res/values folder, which is
# English. AGP needs this to generate the LocaleConfig that gives Android 13
# and up a per-app language entry for the app; without it the
# generateLocaleConfig flag in app/build.gradle.kts fails the build.
unqualifiedResLocale=en-US
