<?xml version="1.0" encoding="utf-8"?>
<input-method xmlns:android="http://schemas.android.com/apk/res/android"
    android:settingsActivity="com.wasimaster.wmkeyboard.app.MainActivity"
    android:supportsSwitchingToNextInputMethod="true">
    <!--
      A single bootstrap subtype, valid before the service registers the real
      per-layout subtypes at runtime (WMKeyboardService.registerSubtypes). It
      carries no layoutId extra value, so selecting it in the OS switcher is a
      harmless no-op; the additional subtypes are what actually switch layout.
    -->
    <subtype
        android:label="@string/subtype_english"
        android:imeSubtypeLocale="en_US"
        android:imeSubtypeMode="keyboard"
        android:isAsciiCapable="true" />
</input-method>
