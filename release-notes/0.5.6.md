A single change, for the F-Droid build.

### Network tools

**The network tools gain hosts anyone can run.** The F-Droid edition ships with
free, self-hostable services behind the tools that need a server, so nothing in
that build depends on a provider you have no account with.
