Type Hindi phonetically, Latin keys in and Devanagari out. English words stay English on Avro and Hindi phonetic. Import themes, layouts and word lists from FlorisBoard, HeliBoard, LeanType, FUTO and AOSP. Chinese types characters. Saved logins land in the strip. The emoji search box completes what you type. Settings rows move instead of jumping. The keyboard fits the screen in landscape. Persian matches the Iranian standard. Dracula gets its light look.

### Installed from F-Droid? Reinstall once

From this version, F-Droid's copy is signed with the same key as the APKs
below: F-Droid still builds it from source, then publishes it only if it is
identical to `wmkeyboard-0.5.10-vc22-fdroid.apk` here. Installs from F-Droid of
0.5.9 or older carry F-Droid's own key, and Android won't update across keys,
so F-Droid can't move you to 0.5.10 by itself. Export a backup
(**Data › Backup & restore**), uninstall, install WM Keyboard from F-Droid
again, and import the backup. Every update after that is ordinary, and you can
move between the F-Droid and GitHub builds without uninstalling.
