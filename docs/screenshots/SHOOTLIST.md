# Screenshot shoot list

216 shots left. Generated by `shootlist.py` — re-run it any time to regenerate with the remaining shots only.

## How to use this

1. Pick one batch below and get to the screen named in its heading.
2. Take the shots **in the listed order**, one device screenshot each (Power + Volume-Down).
3. Don't take any other screenshots in between — order is how they get matched up.
4. Run the import command printed at the top of the batch.
5. If you skip a shot, note which number you skipped and pass `--skip N` (repeatable) to the import command.

Shots go to `/sdcard/DCIM/Screenshots` on the device; the importer pulls the newest N, converts them to WebP, drops them in the right asset folder, swaps the placeholder in the page, and marks the manifest entry done.

---

## Keyboard itself (DocsShotActivity harness)  (124 shots)

```bash
python3 docs/screenshots/import.py keyboard
```

### 1. `tools/ai-panel-idle`

**Shot:** The AI panel's action chips, ready to run against the text in the field.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind multiline --es text 'Reviewing the Q3 roadmap doc before I send it to the team.'
- Configure at least one working AI provider first (otherwise the panel shows the need-setup state instead — see ai-need-setup).
- Tap into the field, open AI from the toolbar/toolbox, screenshot the action-chip row (Rewrite/Improve/Fix grammar/Summarize/Translate/Explain/Continue/Custom) before tapping any chip.

> Needs a configured provider (real key or downloaded local model) so the chips render enabled/colored rather than the empty state.

### 2. `tools/ai-result-ready`

**Shot:** A streamed result with Replace, Insert, and the strip-markdown checkbox.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind multiline --es text 'quick draft text to rewrite'
- Configure a real working provider (cloud key or downloaded local model), tap the field, open AI, tap Rewrite, wait for the response to finish streaming, then screenshot with Replace/Insert and the strip-markdown checkbox visible.

> Needs a live provider response to populate real result text, so this can't be captured offline against a stub setup.

### 3. `tools/calendar-month-grid`

**Shot:** The Calendar panel: month grid with Bengali day numbers, today's events listed below.

- adb shell pm grant com.wasimaster.wmkeyboard android.permission.READ_CALENDAR
- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Focus the field to raise the keyboard, open the toolbox (or tap the pinned Calendar icon if pinned), tap Calendar to open the panel
- Leave today selected so the grid, Friday tint, and today's border/pill are all visible in one frame

> Default settings already show Bengali as First calendar and Hijri as Second, so no settings changes needed before shooting. Grant READ_CALENDAR ahead of time via adb so the panel shows the event list state (even if empty/'No events') rather than the Allow prompt — or leave ungranted if the intent is to show the Allow prompt instead; decide which state this shot is for.

### 4. `tools/calendar-day-events`

**Shot:** A day with events: colored stripe per calendar, time, and location.

- adb shell pm grant com.wasimaster.wmkeyboard android.permission.READ_CALENDAR
- Seed at least 2-3 test events on the device's default calendar for the target day (varying all-day vs timed, one with a location) — no in-app seeding tool exists, use the device's own Calendar app or a content-provider insert script ahead of time
- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open the Calendar panel from the toolbar/toolbox and select the seeded day

> Needs real seeded calendar data — the panel has no demo/mock event mode. Coordinate seed data with whoever captures this.

### 5. `tools/camera-viewfinder`

**Shot:** The live viewfinder: flash and self-timer chips on the left, shutter in the middle, front/back switch on the right.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode chat
- Focus the message field, open the toolbar or toolbox and tap Camera (grant camera permission first if prompted)
- Frame a shot with the back camera, flash off, timer off, before tapping the shutter

> Capture with a device that has both front and back cameras so the switch chip is visible; avoid pinch-zoom (>1.05x) so the zoom pill stays hidden, for a clean baseline shot.

### 6. `tools/camera-confirm`

**Shot:** The confirm step: the captured photo shown exactly where the viewfinder had it, with Retake and Send underneath.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode chat
- Open the Camera tool and tap the shutter to capture a photo
- Screenshot the confirm screen (Retake / Send buttons) before tapping either

> Same DocsShotActivity session as camera-viewfinder; just continue past the shutter tap instead of restarting.

### 7. `tools/camera-permission-gate`

**Shot:** The permission gate shown before camera access is granted, with the Allow camera pill.

- adb shell pm revoke com.wasimaster.wmkeyboard android.permission.CAMERA
- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode blank
- Open the Camera tool from the toolbar or toolbox; screenshot the 'needs permission' message and Allow camera pill without tapping it

> Must run pm revoke first — a normal debug install after granting the permission once won't show this state again without explicitly revoking it.

### 8. `tools/clipboard-panel`

**Shot:** The clipboard panel: staggered two-column cards, entity chips for a fresh code or link, and a search pill.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Before opening the keyboard, copy 3-4 varied clips into the system clipboard in sequence so history is populated: a short text snippet, a longer paragraph, an image (e.g. share a photo to clipboard or copy one in a gallery app), and a link (so a link-preview or raw-URL card shows).
- In WM Keyboard settings, turn on Tools > Clipboard > Detect codes, numbers & links, and copy one more clip that is an SMS-shaped message containing a 6-digit code and a phone number, so the entity strip has something to show.
- Focus the text field and tap the Clipboard icon on the toolbar to open the panel.

> Needs a populated, varied history (text/image/link/entity-bearing clip) to look representative — a fresh install's panel is empty. Capture with the search bar off (default) so the full grid + entity strip show together.

### 9. `tools/clipboard-info-popup`

**Shot:** Long-pressing a clip: the info popup shows when it was copied, its type, size, and source app.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- In WM Keyboard settings, turn on Tools > Clipboard > Show source app (grant Usage Access when prompted) so the popup's From row has something to show.
- Copy a text clip, open the Clipboard panel from the toolbar, then press and hold that clip's card until the info popup appears.

> Turning on 'Show source app' first makes the popup's optional From row appear in the shot; without it the popup is one row shorter (Copied/Type/Size only).

### 10. `tools/clipboard-rich-media`

**Shot:** A file, video, and folder clip side by side — each keeps only a reference to the source, not the bytes.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- From a file manager app, copy a document/PDF file, a short video file, and a folder in sequence onto the system clipboard.
- Open the Clipboard panel from the toolbar and scroll until a file card, a video card (with its extracted frame), and a folder card are all visible together, then crop/frame the shot around those three cards.

> May need to scroll the staggered grid to get all three kinds in one frame since they interleave with other history; consider a taller crop or capturing with minimal other history so these three are near the top.

### 11. `tools/clipboard-entity-chips`

**Shot:** Entity chips above the history grid: an OTP and a phone number pulled out of a longer message.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Ensure Tools > Clipboard > Detect codes, numbers & links is on (default).
- Copy an SMS-shaped text clip containing both a 6-digit verification code and a phone number, e.g. 'Your code is 482913. Call us at 555-201-4488 if you did not request this.'
- Open the Clipboard panel from the toolbar; the dashed-outline chip strip should appear above the history grid with an OTP chip and a phone chip.

> Keep the sample message realistic but obviously synthetic (no real phone number/PII). The chip strip is hidden while the search bar is active, so make sure search is not open for this shot.

### 12. `tools/handwriting-panel-writing`

**Shot:** The handwriting panel: a writing canvas with a delete, space, enter, and close key down the right side.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode blank --es theme dark
- Tap the blank field to summon WM Keyboard, then open the Handwriting tool from the toolbar (or the toolbox if it isn't pinned).
- Draw a short word on the canvas so the ink, the dimmed-while-recognizing state or a settled stroke, and the bottom-left "Undo last stroke" icon are all visible.
- Optional: enable 2+ languages that each have an ML Kit model first, so the language chip also shows in the top-left corner.

> Panel is reached via the toolbar, not a settings deep link. HandwritingPanel.kt: canvas + 64dp action rail (Delete/Space/Enter/Back), undo icon only shows once hw.strokes is non-empty and status == READY, language chip only shows when 2+ distinct ML Kit tags are enabled.

### 13. `tools/text-edit-panel`

**Shot:** The text-editing panel: a d-pad for moving the cursor, Select/Select all/Copy/Paste on the right, and Home/End/Backspace along the bottom.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind multiline --es text 'The quick brown fox jumps over the lazy dog.\nA second line of sample text to edit.\nAnd a third line so cursor and selection moves are visible.'
- Open the toolbar/toolbox and tap the Text editing tool.
- Optional: tap Select, then Right a couple of times, to also show the active Select state and a highlighted selection in the field behind the panel.

> Text editing is not pinned to the toolbar by default (default pinned row is Emoji/Clipboard/Settings) — open it from the toolbox overflow, or pin it first via Settings > Tools.

### 14. `tools/numpad-panel`

**Shot:** The numpad panel: a phone-style or calculator-style 4×4 grid, with Backspace repeating on hold.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open the toolbar/toolbox and tap the Numpad tool.
- Capture the default calculator-style layout (7 8 9 on top). Phone-style layout is opt-in under Settings > Tools > Numpad > Options.

> Numpad is a manually-opened panel usable over any field, distinct from the automatic numeric layout that appears in genuinely numeric fields (field-adaptation, documented on typing/field-adaptation.mdx) — do not use kind=number for this shot, a plain text field better shows it's a manual override.

### 15. `tools/translate-panel`

**Shot:** The Translate panel mid-translation, target-language chip and the Insert/Replace actions below it.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open the keyboard, open the toolbox, tap Translate
- Type a short sentence into the panel's search bar and wait for the live translation to appear (or press Enter to force it)
- Optionally tap the target-language chip once first so a non-English target is visible in the shot

> Panel is self-contained (types into its own search bar, never reads the field) so the underlying field content is irrelevant — any short field text is fine. Capture after translated text is showing so both action buttons (Replace text / Insert) are in their enabled (non-dimmed) state.

### 16. `tools/voice-panel-listening`

**Shot:** The voice panel: a pulsing mic button on the left, the handwriting-style action rail on the right.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open the keyboard, tap the toolbar Voice typing tool (or toolbox if unpinned) to open the full panel
- Tap the mic and speak briefly so status is LISTENING (pulse ring visible) when capturing

> Compact-bar mode must be off (default) to get the full panel. Capture mid-listening so the pulse ring around the mic and a bit of partial/status text under it are visible. Rail shows Delete/Space/Enter(or search icon for a search field)/Back-to-keyboard.

### 17. `tools/voice-undo`

**Shot:** The Undo control, shown after a phrase commits.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open Voice typing panel, dictate one short phrase and let it commit (with Keep listening off, or capture right after a commit before the next session starts)
- Undo row appears bottom-left of the panel once the phrase has committed and status is idle

> Undo icon+label is only visible when voice.canUndo is true and status is not LISTENING/FINISHING/TRANSCRIBING — commit a phrase, then screenshot before dictating again.

### 18. `tools/voice-offline-model-chip`

**Shot:** Offline model download chip in the voice panel.

- Requires a device on API 33+ (Android 13) where the active language's on-device speech model is not yet installed
- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open Voice typing panel with English (or Bengali) as the active layout; the 'Get English for offline dictation' chip should appear under the status text automatically when VoiceModelState is DOWNLOADABLE

> Device/emulator dependent — chip only renders when SpeechRecognizer.checkRecognitionSupport reports DOWNLOADABLE for that language, which needs API 33+ and Google app support. May need a physical device rather than emulator; document as best-effort if unobtainable, and drop to a text-only fallback description if capture proves infeasible.

### 19. `tools/media-now-playing`

**Shot:** Media controls showing album art, track info, a seek bar, and the play/pause/skip transport.

- Grant WM Keyboard notification-listener access in system Settings first (Settings > Apps > Special app access > Notification access, or via the in-panel Grant access flow) so a real session can be mirrored.
- am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode blank --es theme dark
- Start audio playback in any media app with a session (e.g. YouTube Music, a podcast app) with album art available.
- Focus the DocsShotActivity field to raise the keyboard, then open Media controls from the toolbox (or leader key: double-tap Ctrl, then M).

> Needs a real active MediaSession with album art, a title/artist/album, and a nonzero duration so the seek bar renders. Pick a track with distinct art so the placeholder vs real-art states are visually obvious in other shots if needed.

### 20. `tools/media-access-prompt`

**Shot:** The access prompt shown before notification-listener access is granted.

- Ensure WM Keyboard notification-listener access is NOT granted (revoke it in Settings > Apps > Special app access > Notification access if previously granted).
- am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode blank --es theme dark
- Focus the field, open Media controls from the toolbox; the panel shows the notification-bell icon, explanatory text, and a 'Grant access' button since access hasn't been granted yet.

> Must be captured with the grant revoked — re-revoke after capturing tools/media-now-playing if using the same device/session.

### 21. `tools/toolbar-and-toolbox`

**Shot:** The toolbar's default row (Emoji, Clipboard, Settings) with the toolbox grid open below it.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Tap the toolbox (grid) button on the toolbar to expand it before capturing

> Capture with default pinned tools (Emoji, Clipboard, Settings) and the toolbox panel expanded beneath the toolbar so both pinned and unpinned tools are visible in one shot.

### 22. `tools/ocr-viewfinder`

**Shot:** Text scan (OCR): a full-bleed camera view with recognized words ready to tap.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open the keyboard, tap the toolbox, open Text scan (OCR), point at any printed text so the viewfinder is live

> Panel is full-bleed and covers the toolbar row too — frame the whole keyboard area, not just the key rows. Torch icon should be visible if the test device has a flash.

### 23. `tools/ocr-result-chips`

**Shot:** OCR result: recognized words as chips, grouped by line, ready to copy or insert.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open Text scan (OCR), capture a printed English paragraph, wait for recognition to finish and show the chip result screen

> Default state (Start with everything selected = on) has all chips pre-selected; capture that default look unless a deselected-state shot is separately wanted.

### 24. `tools/qr-scan-live`

**Shot:** QR & barcode scanner: live camera view with pinch-to-zoom, freezing on the first code it decodes.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open QR & barcode scanner, point at a QR code (a URL-encoded one shows the Open button too) until it freezes on the result card

> Prefer a URL-encoded QR code so the Open button and any link-preview card are visible if Load link details is enabled for the shot.

### 25. `tools/doc-scan-capture`

**Shot:** Document scanner: Google's scan UI for capturing and cropping a page.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open Document scanner from the toolbox; this launches Google's own GmsDocumentScanning full-screen activity outside the keyboard — capture that screen directly, not the keyboard panel

> This is Google's own scanner UI (not WM Keyboard chrome), so it may need Play services present and a physical/emulated camera feed with a document in frame. Confirm host classification is still appropriate since the UI is a separate system activity, not the DocsShotActivity field itself.

### 26. `tools/web-search-results`

**Shot:** Web search results — tap a row to insert its link, or tap the open icon to launch it in your browser.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Focus the field, open the toolbox (WM Keyboard isn't pinned by default), tap Web search, type a short query into the panel's own header search bar, and press the keyboard's search/enter action to load results (it does not search live as you type).
- Needs a usable Brave API key configured (user key or a build-baked BuildConfig.BRAVE_API_KEY) or results will not load — see the search-needs-key entry for the no-key state instead.

> Capture with at least 2-3 results visible so both the insert-by-tap row and the separate open-in-browser icon (top right of each row) are visible.

### 27. `tools/image-search-grid`

**Shot:** Image search — tap a thumbnail to insert the image itself; long-press to insert its link instead.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open the toolbox, tap Image search, type a query into the header search bar, press the search/enter action.
- Needs the same Brave key requirement as web search.

> 3-column grid; capture with enough thumbnails to fill at least two rows.

### 28. `tools/search-needs-key`

**Shot:** The web search panel before a Brave API key is configured.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Requires a build with no baked-in Brave key (BuildConfig.BRAVE_API_KEY blank) and no user key pasted into Tools / Web search settings.
- Open the toolbox, tap Web search — the panel should show the "Web search needs an API key — Brave Search. Add one in the tool's settings." notice with an Open settings button.

> Verify the debug/docs build variant actually ships without a baked Brave key before capturing, otherwise this state won't reproduce.

### 29. `tools/wikipedia-article`

**Shot:** An open Wikipedia article, with its Summary, Links and Full article tabs.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open the toolbox, tap Wikipedia, type a well-known topic into the header search bar, press search/enter, tap a result to open its article.
- Capture on the Summary tab (default) with the Insert summary / Insert link / Insert title chip row and the three tab chips (Summary, Links, Full article) all visible in the header.

> No API key needed — this tool works with no configuration at all.

### 30. `tools/dictionary-entry`

**Shot:** A dictionary entry — serif headword, pronunciation audio, numbered definitions and synonym chips.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text --es text 'The quick brown fox'
- Place the cursor inside an English word (e.g. "quick"), open the toolbox, tap Dictionary — with auto-lookup on (the default) it should look up that word immediately.
- Pick a word whose entry has a pronunciation-audio icon and at least one synonym/antonym chip row for the fullest capture.

> No API key needed. The headword renders in the Lora serif font — verify it actually downloaded before capturing, or the shot will show the system-serif fallback instead.

### 31. `tools/snippets-panel`

**Shot:** The snippets panel: saved snippets as two-column cards, ready to insert with a tap.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text --es hint 'Message'
- Before shooting, add 3-4 sample snippets from Settings -> Tools -> Snippets so the grid isn't empty (short labels, 1-2 line text; include one whose text is long enough to ellipsize at 2 lines).
- Focus the field to bring up the keyboard.
- Snippets is not pinned to the toolbar by default (only Emoji, Clipboard, Settings are) -- open the toolbox and tap Snippets, or type the keyword 'snippets' and tap the resulting smart-chip suggestion.

> Panel is a LazyVerticalGrid, columns=2, 8dp spacing. Each card shows snippet.label (bold, 1 line, ellipsized) and snippet.text (2 lines, ellipsized) -- the RAW template text, not the expanded preview, so a snippet containing {date} should show the literal token in the shot, not today's date. Header row above the grid reads 'Variables: {date} {time} {datetime} {clip}' with a gear icon.

### 32. `emoji/word-suggestions`

**Shot:** Emoji suggestions appended to the strip after typing a whole word.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Switch to WM Keyboard as the active input method
- Type "birthday" and pause before hitting space so the suggestion strip shows the trailing emoji

> Requires Emoji suggestions on (default true) and English enabled (default). Expect roughly 🎂🎉🥳🎁 appended after the word per EmojiSuggester's TRIGGERS map.

### 33. `emoji/inline-colon-search`

**Shot:** Typing a colon turns the suggestion strip into emoji search results.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Type ":cat" and pause so the strip shows emoji search results in place of word suggestions

> Requires Inline emoji search on (default true, lives under Typing settings, not Emoji settings).

### 34. `emoji/emoji-row-own`

**Shot:** The emoji row in Own row mode, above the key rows.

- In WM Keyboard settings, go to Emoji, set "Emoji row" to "Own row"
- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Switch to WM Keyboard and let it render with the emoji row visible above the key rows

> Default emoji row mode is Button, not Own row — must switch it first. Revert to Button afterward if the test device is reused for other shots.

### 35. `emoji/gif-panel-trending`

**Shot:** The GIF panel, showing trending results on open.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode chat
- Focus the message input to raise the keyboard.
- Open the toolbar (or toolbox) and tap the GIF tool — trending results load automatically with no query typed.
- Ensure a Klipy or GIPHY API key is configured first (Settings > Tools > GIFs) or the panel shows the NeedKey notice instead of a grid.

> 3-column animated grid; header shows 'via Klipy · GIPHY' attribution line above the grid depending on which keys are configured.

### 36. `emoji/gif-no-rich-media`

**Shot:** A field that can't accept images — the panel dims and shows the standing notice.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Focus the plain text field (kind=text does not advertise image/* content MIME types).
- Open the GIF tool from the toolbar/toolbox so results still populate.
- Capture with the grid visible at ~45% opacity and the 'This field doesn't accept GIFs — try a chat or notes app' notice showing above it.

> Notice only appears when the search box is not active/expanded (state.mediaSearchActive == false) — make sure the search field is collapsed before capturing.

### 37. `emoji/gif-long-press`

**Shot:** Long-press menu over a GIF result: Copy and Report.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode chat --es theme dark
- Open the GIF tool, search or browse trending so a Klipy/GIPHY result tile is visible
- Long-press that tile to open the MediaActionSheet scrim/menu and capture with it open

> The GIF version of the menu has no save-to-pack rows (a GIF can't become a sticker), so it should read as exactly two rows: Copy and Report.

### 38. `emoji/kaomoji-tab`

**Shot:** The emoji panel's Kaomoji tab, grouped by mood.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text theme=dark
- In-app: no deep link exists for the Emoji settings screen (SettingsShortcuts.Routes only allows typing, appearance, themes, languages, tools, search) — from Settings home tap Emoji, then in the 'Emoji panel' group turn on 'Kaomoji and Emoticons'
- Return to the DocsShotActivity field, tap the emoji toolbar button to open the emoji panel
- Swipe/scroll the tab strip to the end and tap the '^_^' tab (KAOMOJI_TAB)
- Scroll to the top so the Happy group header is visible

> kaomojiTabs defaults to false (EmojiSettings.kaomojiTabs, SettingsRepository.kt:1541) so it must be enabled first. The tab strip label is a literal glyph '^_^', not an icon.

### 39. `emoji/kaomoji-mood-groups`

**Shot:** Scrolling past the Table flip and Confused groups.

- Same setup as emoji/kaomoji-tab (enable Kaomoji and Emoticons in Settings > Emoji > Emoji panel, open the emoji panel, tap the ^_^ tab)
- Scroll the grid down until the 'Table flip' and 'Confused' section headers are both visible on screen

> Demonstrates the full-width mood-group headers (11sp, 70%-alpha onSurfaceVariant) inside the LazyVerticalGrid. Table flip is the smallest group (8 entries) so it scrolls past quickly.

### 40. `emoji/panel-overview`

**Shot:** The emoji panel open over a text field, with the search icon, tab strip, and category grid.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Focus the field to raise the keyboard
- Tap the Emoji toolbar button (toolbar right side) to open the panel on a mid-catalog category tab, e.g. Smileys

> Full-screen emoji panel (emojiFullBleed) defaults to on, so the panel will show the reclaimed tab-strip-plus-back-button layout, not the toolbar-visible layout, unless that setting is turned off first for a more toolbar-illustrative shot.

### 41. `emoji/panel-search`

**Shot:** Typing 'cat' and 'বিড়াল' in the panel's search field, both surfacing the same cat emoji.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open the emoji panel, tap the search (magnifying glass) tab
- Type 'cat' on the letter keys (they feed the search pill while search is active) and capture the result grid; optionally take a second pass with a Bengali-layout active typing বিড়াল for the side-by-side claim

> May need two source captures (English query, Bengali query) composited into one caption if the doc wants both shown at once; a single English-query still is an acceptable fallback.

### 42. `emoji/variant-popup`

**Shot:** Long-pressing a toned emoji opens six variants: the neutral base plus five Fitzpatrick tones.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open the emoji panel, scroll to a single-tone emoji such as 👍 (thumbs up) in the People or Smileys category
- Long-press the cell to open EmojiVariantPopup and capture while the popup is showing

> Turn on 'Emoji descriptions' first so the popup's name header is visible in the shot, matching the caption text in the page.

### 43. `emoji/dual-tone-picker`

**Shot:** The dual-tone picker for 🤝, with independent skin tones for each hand.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open the emoji panel, find 🤝 (handshake) in the People category
- Long-press to open DualTonePicker and capture with at least one tone picked on each side so both swatch rows show a selection ring

> Picking a tone on one side auto-seeds the other side (not every combo is RGI-valid) -- capture after both rings are visibly set to show the two independent rows clearly.

### 44. `emoji/history-tab`

**Shot:** The history tab with favourites pinned first, and the long-press options for a starred emoji.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Use the keyboard to insert a handful of different emoji first so the history tab is populated, and favourite at least one via its long-press popup
- Reopen the emoji panel (history tab is the first tab) and long-press a favourited cell so the popup's Favourited/Reorder/Remove rows are visible in the same shot

> The history tab only appears once there is at least one recent or favourited emoji -- a completely fresh install/profile will not show it.

### 45. `emoji/favourites-reorder`

**Shot:** Dragging a favourite into a new position in the reorder list.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Favourite at least two emoji via their long-press popups
- Long-press a favourited cell, tap 'Reorder favourites', and capture mid-drag (or immediately post-drop) in FavouritesReorderPopup

> Reorder row only appears in the long-press popup once 2+ favourites exist. A mid-drag capture is nice-to-have but a static list with the drag handles visible is an acceptable still.

### 46. `emoji/sticker-panel-search`

**Shot:** Sticker search results, with the My stickers tab for your own packs.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode chat --es theme dark
- Open the keyboard, open the Stickers tool (toolbox if not pinned), and either search a term or leave the default trending grid showing
- Ensure at least one custom pack exists beforehand if the My stickers chip should be visible in the shot

> Needs a Klipy or GIPHY API key configured on the capture device/build, or the panel will show the NeedKey notice instead of results.

### 47. `emoji/sticker-panel-unsupported-field`

**Shot:** The standing notice a field that can't take images shows, with results dimmed underneath.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text --es theme dark
- Open the keyboard, open the Stickers tool with an active search so results are populated
- Confirm the 'This field doesn't accept stickers — try a chat or notes app' notice shows above the dimmed grid

> Plain text field's EditorInfo advertises no image content MIME types, so acceptsRichMedia is false — this is the default DocsShotActivity field kind, no extra flag needed beyond kind=text.

### 48. `emoji/sticker-long-press-save`

**Shot:** Long-press menu over a Klipy result: save to a pack, copy, or report.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode chat --es theme dark
- Open the Stickers tool, search or browse trending so a Klipy/GIPHY result tile is visible
- Long-press that tile to open the MediaActionSheet scrim/menu and capture with it open (Save rows, then Copy and Report)

> Capture once with zero existing packs (menu shows 'Save to a new pack', Copy, Report) and optionally a second variant with 1-2 packs to show the 'Save to <pack name>' rows — page only needs one still, pick whichever reads clearer. A My-stickers tile shows a shorter menu (Manage packs, Copy) and is not this shot.

### 49. `start/editions-toolbox-full`

**Shot:** The Tools panel on a Full build: Handwriting, Text scan, QR & barcode, Document scanner and Grammar check all appear.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Requires a Full-flavor install (app-full-*-release.apk or -debug.apk)
- Focus the field to raise the keyboard, tap the toolbar's toolbox icon to open the Tools panel
- Screenshot the panel with the tool grid visible, including Handwriting/Text scan (OCR)/QR & barcode scanner/Document scanner/Grammar check

> Which tools show is compile-time (BuildConfig flags), not a runtime setting, so this shot and editions-toolbox-lite need two separate app installs, not two states of one install.

### 50. `start/editions-toolbox-lite`

**Shot:** The same panel on a Lite build — those five tiles are gone, not grayed out.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Requires a Lite-flavor install (app-lite-*-release.apk or -debug.apk) instead of Full
- Focus the field, tap the toolbar's toolbox icon to open the Tools panel
- Screenshot the same panel — same tool grid minus the five Full-only tiles

> Pair with editions-toolbox-full for the before/after; frame the two crops identically (same panel scroll position) so the diff reads clearly side by side in the page.

### 51. `start/first-launch`

**Shot:** First launch after install, before the setup wizard starts.

- Install a debug APK (assembleFullDebug) on a clean profile / after clearing app data so onboardingDone is false
- Launch the app cold and capture the very first frame before the onboarding wizard's WelcomePage finishes composing, or capture WelcomePage itself if the pre-wizard frame is too transient to be a meaningful shot

> This is mostly a placeholder for 'what a fresh install looks like on open' — if the true first frame is not a distinct visual from the onboarding WelcomePage (MainActivity.kt / Onboarding.kt), it's fine to just capture WelcomePage and adjust the caption to say so; don't invent a distinct pre-wizard screen if one doesn't exist.

### 52. `start/migrating-suggestion-center`

**Shot:** Suggestions default to the top pick in the center slot, the same layout Gboard uses.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Type a word that yields multiple candidates (e.g. "hte") so three suggestions appear with the top pick centered
- Capture before tapping any suggestion

> Confirms suggestionPrimaryCenter=true default (SettingsRepository.kt:1820), explicitly commented '(Gboard style)' in source.

### 53. `start/setup-card-not-enabled`

**Shot:** The setup card before WM Keyboard is enabled — 'Enable keyboard' opens system settings, 'Switch keyboard' opens the picker.

- Use a device/emulator where WM Keyboard is installed but not yet enabled as an input method (fresh install, or `adb shell ime disable com.wasimaster.wmkeyboard/.ime.WMKeyboardService` if it was enabled before)
- am start -n com.wasimaster.wmkeyboard/.app.MainActivity
- Capture the Home screen's top card in its 'Not enabled' state (title 'Setup', both 'Enable keyboard' and 'Switch keyboard' buttons visible)

> This is the MainActivity Home screen, not the IME field harness — DocsShotActivity does not apply. Card polls IME state every ~1s so it will flip states live if you enable the keyboard mid-session; capture before enabling. A second still of the 'Enabled and selected' checkmark state would also be useful but is not required for this page.

### 54. `tour/anatomy-default`

**Shot:** Fresh install: the top bar shows pinned tools, not an empty suggestion strip, until you type something.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text

> Default state, no text typed yet, so TOPBAR resolves to toolbar (not suggestions). Symbol row and dedicated emoji row are both off by default in this shot; showToolbar=true because suggestionsFirst=false and there is nothing to suggest.

### 55. `tour/toolbar-pinned`

**Shot:** The toolbar: back-to-keyboard chevron, the Toolbox launcher, then your pinned tools.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- type a few characters so the back chevron is not needed, or crop to show the toolbar at rest with default pinned tools (Emoji, Clipboard, Settings) plus the Toolbox + button

> Default toolbarTools = [EMOJI, CLIPBOARD, SETTINGS]. No panel open so the back chevron should NOT show; capture the resting toolbar state.

### 56. `tour/toolbox-grid`

**Shot:** The Toolbox grid — every enabled tool, four to a row, drag to pin or reorder.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- tap the toolbar's Toolbox (+) button to open PanelMode.TOOLBOX

> toolboxColumns=4. Capture with the first-run drag hint banner visible if easy (toolboxHintDismissed default false) since it's part of the real first-open experience.

### 57. `tour/long-press-alternates`

**Shot:** Long-pressing a key with alternates pops up the accented and symbol variants.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- long-press a letter key with known alternates (e.g. 'e' or 'a') and hold until the alternates popup renders, then capture

> longPressDelayMs default 300ms; longPressHints default true so the corner hint mark should also be visible on nearby keys in the same shot.

### 58. `languages/cjk-pinyin-candidate-strip`

**Shot:** Chinese Pinyin: the candidate strip replaces the usual word-suggestion row while you're composing a reading.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Enable Chinese (Pinyin layout) in Languages, switch to it, type a multi-syllable reading like 'nihao' so the candidate strip is populated with several ranked candidates

> Pinyin dictionary pack must be downloaded first (Languages > Chinese > Chinese options) or the strip will be empty.

### 59. `languages/cjk-cangjie-keys`

**Shot:** Cangjie: radical keys build a shape code; the strip fills in once the code is complete.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Switch to the Chinese Cangjie layout and type 2-3 radical keys so the composing region shows radical glyphs and the strip shows candidates

> Cangjie dictionary pack must be downloaded first.

### 60. `languages/cjk-japanese-flick`

**Shot:** Japanese Flick: kana glyphs sit on the keys themselves, with directional flicks for the rest of the row.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Enable Japanese, switch to the Flick layout, show the idle keyboard (12-key pad with kana glyphs on keys)

> Static keyboard shot is fine; the flick gesture itself is better shown as a kind:anim companion if a future pass wants motion.

### 61. `languages/cjk-candidate-grid-expanded`

**Shot:** The expanded candidate grid: every ranked candidate, wrapped to fit, covering the key rows while it's open.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Type a Pinyin or Jyutping reading with many candidates, then tap the chevron at the end of the candidate strip to open the expanded FlowRow grid

> Needs the relevant conversion dictionary downloaded so the grid has enough candidates to show wrapping.

### 62. `languages/fancy-text-bold-field`

**Shot:** The Bold style active in a text field: typing hello commits 𝐡𝐞𝐥𝐥𝐨 directly.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Enable Fancy Text and its Bold layout (Settings > Languages > Add language > search fancy > tap the row, which auto-enables Bold) or switch to it with the globe key if already enabled
- Type a short word, e.g. hello, so the composed 𝐡𝐞𝐥𝐥𝐨 is visible and legible in the field

> Keep the typed word short and common so the styled glyphs stay readable at field size; avoid characters from the 'hole' letters (C,H,I,N,P,Q,R,T,Z) since Bold itself has no holes anyway.

### 63. `languages/notation-music-field`

**Shot:** Musical notation active in a text field: note durations, rests, and clefs on the key grid.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Enable Music notation via Settings > Languages > Add language > search 'music', switch to it with the globe key, tap the field, capture.

> Show the main letters layer of music.wmlayout.json (durations/rests/clefs row) in dark theme default.

### 64. `languages/notation-braille-chord`

**Shot:** Braille active: chording dots 1 and 4 together commits the letter c.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Enable Braille via Settings > Languages > Add language > search 'braille', switch to it, tap the field.
- Capture mid-chord if possible (two fingers held on dot1/dot4); if a live mid-press screenshot isn't practical, capture the idle six-key grid instead and adjust the caption.

> Chord press-state may be hard to freeze for a still capture — a short screen recording cropped to a frame is an acceptable fallback for the capture agent.

### 65. `languages/notation-morse-strip`

**Shot:** Typing morse: the strip shows the sequence so far and, live, the letter it currently spells.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Enable Morse code via Settings > Languages > Add language > search 'morse', switch to it, tap the field.
- Tap the dot key twice (short of the 750ms commit pause) so the suggestion strip shows the live '··   i' readout, then capture before it commits.

> Timing-sensitive: capture must land inside the 750ms window after the last tap. A short screen recording trimmed to a frame is a safer fallback than a live single-shot screenshot.

### 66. `languages/spacebar-resting-label`

**Shot:** The resting spacebar shows the current language's name, with switch-direction arrows on either side.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Set WM Keyboard as the active IME and focus the field with default settings (4 layouts enabled, spacebarLanguageArrows on, spaceShortSwipe = Language).
- Capture the resting keyboard without touching the spacebar.

> Arrows only draw when a swipe slot is set to Language and more than one layout is enabled — both true by default, so this should show them.

### 67. `languages/spacebar-language-preview`

**Shot:** Mid-swipe: the language preview shows every enabled layout, current selection highlighted.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Touch down on the spacebar and drag horizontally past the swipe threshold without lifting.
- Capture while the preview popup is visible above the spacebar, before releasing.

> Default enabled set is 4 layouts (QWERTY/Avro/Probhat/Jatiya) so the preview shows all 4 chips, no sliding window needed. Current selection is bold, accent-tinted and pill-highlighted; others at ~45% opacity.

### 68. `languages/hold-drag-picker-list`

**Shot:** More than four languages enabled: holding the spacebar opens a scrollable list instead of the inline preview.

- Settings deep link: am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages
- Tap Add language and enable a 5th layout so enabledLayoutIds.size > 4 (the list threshold).
- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Press and hold the spacebar past ~250ms, drag down through a couple of rows, and capture with the list popup open and a row highlighted.

> With the shipped default of exactly 4 enabled layouts and quick swipe on Language, holding shows the inline preview (see spacebar-language-preview) instead — this list only appears past 4 layouts, or if the quick-swipe slot is reassigned off Language. Needs the settings deep-link step first to add a 5th language.

### 69. `privacy/incognito-toolbar-badge`

**Shot:** The incognito badge next to the toolbar, and the toolbox tool that turns it on.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Focus the field so the keyboard shows
- Open the toolbox (grid button on the toolbar) and tap the Incognito tool to turn it on — it is not pinned to the toolbar by default (DefaultToolbarTools = EMOJI, CLIPBOARD, SETTINGS; ToolbarTool.kt:131-132), so find it in the toolbox grid
- Close the toolbox; the incognito badge (IconSlots.CHROME_INCOGNITO, KeyboardScreen.kt:3656-3665) now shows next to the toolbar and the Incognito tool cell reads as active

> Confirms onIncognitoToggle() (WMKeyboardService.kt:6277-6297) and the badge render condition state.incognitoOn.

### 70. `privacy/clipboard-sensitive-masked`

**Shot:** A sensitive clip, masked in the clipboard panel.

- Before capture, copy a token that trips ClipSensitivity.looksSensitive() from any app — e.g. a mixed-case/digit/symbol string 8-128 chars long such as 'aB3!kZ9xQ7z', or a bare 4-8 char alphanumeric code with a digit such as 'G4K7QP' (must be the whole clip, no surrounding text)
- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Focus the field, open the toolbar/toolbox, tap the Clipboard tool to open the panel
- The sensitive clip renders masked behind a lock icon with a hidden character count instead of its text (ClipSensitiveBody, KeyboardScreen.kt:9707-9735)

> Works with default settings (sensitiveHandling=SHORT_LIVED, detectSensitive=true) — no settings changes needed, just a qualifying copy beforehand.

### 71. `typing/glide-trail`

**Shot:** A glide in progress: the comet-style trail follows the stroke and fades behind it.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- adb shell settings put system show_touches 1
- adb shell screenrecord --time-limit 6 --bit-rate 8M /sdcard/rec.mp4 &
- adb shell input swipe <h-key-x> <h-key-y> <o-key-x> <o-key-y> 600   # trace a slow multi-letter glide, e.g. h-e-l-l-o
- wait; adb pull /sdcard/rec.mp4
- ffmpeg -i rec.mp4 -ss 0.5 -t 3.5 -vf "fps=15,scale=540:-1" -c:v libwebp_anim -loop 0 -q:v 70 -an glide-trail.webp

> Animated WebP — the trail only reads in motion. show_touches on so the finger dot is visible alongside the trail. Requires a still sibling for reduced-motion readers (see typing/glide-trail-still below); both entries share the same placeholder id/comment in the page.

> **Animated** — record a short clip instead of a still.

### 72. `typing/glide-live-preview`

**Shot:** Mid-swipe: the suggestion strip already shows the leading candidate before the finger lifts.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- adb shell input swipe <h-key-x> <h-key-y> <o-key-x> <o-key-y> 600 &
- adb exec-out screencap -p > glide-live-preview.png   # capture partway through the swipe, before the finger lifts
- cwebp -q 88 glide-live-preview.png -o glide-live-preview.webp

> Timing-sensitive still capture (same caveat as the glide-trail-still entry) — needs the screencap to land mid-swipe, before input swipe's synthetic finger-up. May require status: blocked with a manual capture note if adb timing proves unreliable.

### 73. `typing/glide-alternates`

**Shot:** After the swipe lands, alternates sit either side of the committed word in the suggestion strip.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- adb shell input swipe <h-key-x> <h-key-y> <o-key-x> <o-key-y> 400
- adb exec-out screencap -p > glide-alternates.png
- cwebp -q 88 glide-alternates.png -o glide-alternates.webp

> Capture right after finger-up so the committed word is bolded center with two alternates flanking it in the suggestion strip.

### 74. `typing/glide-multiword`

**Shot:** One continuous stroke crossing the spacebar mid-glide, committing a full phrase.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- adb shell input swipe <h-key-x> <h-key-y> <space-x> <space-y> 300
- adb shell input swipe <space-x> <space-y> <t-key-x> <t-key-y> 300   # continue into a second word across the spacebar
- adb exec-out screencap -p > glide-multiword.png
- cwebp -q 88 glide-multiword.png -o glide-multiword.webp

> Two chained input swipe calls approximate one continuous stroke; may need adb shell input motionevent DOWN/MOVE/UP sequencing instead if separate swipes don't register as one glide. Show the committed phrase in the text field, not mid-gesture.

### 75. `typing/hardware-typing-live`

**Shot:** Typing on a physical keyboard: a word underlined mid-correction in the field.

- Attach a Bluetooth/USB keyboard, or in an emulator enable Extended controls > Keyboard > 'Show taps'/hardware keyboard passthrough.
- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Ensure Typing > Physical keyboard > 'Process hardware keys' is on and a phonetic/suggestion-generating language is active, then type on the physical keyboard until autocorrect underlines a word.

> Needs an actual physical/emulated hardware keyboard input source, not just the on-screen IME; DocsShotActivity only sets up the focused field.

### 76. `typing/hardware-tool-picker`

**Shot:** The tool-picker overlay after double-tapping Ctrl, showing the bound letters.

- Attach a hardware keyboard (see hardware-typing-live setup).
- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Double-tap the physical Ctrl key to arm the picker, then screenshot immediately (it auto-disarms after about 3 seconds).

> Timing-sensitive capture; the overlay is drawn over the top strip, not a full panel.

### 77. `typing/hardware-focus-ring`

**Shot:** The focus ring highlighting a result inside an open panel after an arrow-key press.

- Attach a hardware keyboard (see hardware-typing-live setup).
- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Double-tap Ctrl, press E to open the Emoji tool, then press an arrow key to draw the focus ring on a result.

> Ring is a 2dp accent outline plus 16%-alpha accent fill; only ever drawn after a key press, never in touch mode.

### 78. `typing/symbol-row-picker`

**Shot:** The symbol row above the key grid, with its set picker open.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- In Settings, turn on Rows & bars > Symbol row (off by default) before this shot
- Tap the field, then tap the chip at the left edge of the symbol row to open the set dropdown

> SymbolRowStrip, KeyboardScreen.kt:2394-2466. Dropdown only appears/is clickable when more than one symbol set is enabled (all 5 are enabled by default).

### 79. `typing/modes-tool-panel`

**Shot:** The Modes tool: Automatic at the top, then one row per mode with its auto-activation summary.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Tap the field, open the toolbox, and open the Modes tool

> ModesPanel, KeyboardScreen.kt:2494-2570+. Shows the leading 'Automatic — Follow each mode's app and field bindings' row followed by the 6 seeded modes with icon/name/modeSummary subtitle.

### 80. `typing/modes-manual-picker`

**Shot:** The Modes tool open, showing the manual override list.

- Same as typing/modes-tool-panel — this can reuse the same capture or a near-duplicate with a mode row highlighted/selected

> Duplicate/alternate framing of the Modes tool; consider merging with modes-tool-panel if the capture agent judges one shot covers both captions.

### 81. `typing/power-saving-toolbar-toggle`

**Shot:** Tapping the Power saving toolbar tile — the toast confirms it switched on.

- am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open the keyboard, tap the toolbox, and pin Power saving to the toolbar first if it isn't already there (it is not a default or recommended tool)
- Tap the Power saving toolbar tile once to trigger the 'Power saving on' toast, and capture while the toast is visible

> Power saving is not in DefaultToolbarTools or RecommendedTools, so it must be dragged into the toolbar from the toolbox before it can be tapped. The toast is transient (Toast.LENGTH_SHORT) so timing the capture matters.

### 82. `typing/size-position-floating-resize`

**Shot:** Dragging the resize grip changes width and height together.

- Same floating-keyboard setup as typing/size-position-floating
- Record a short loop: drag the resize grip up-and-out to grow the panel, since dragging up on the top-bar grip grows it (KeyboardScreen.kt:1019-1032), then let it settle back

> Motion-only behavior; typing/size-position-floating is the required still/reduced-motion sibling for this same feature.

> **Animated** — record a short clip instead of a still.

### 83. `smart/autocorrect-confidence-fix`

**Shot:** A mistyped word corrected inline as the next word begins.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text --es theme dark
- Focus the field, open WM Keyboard, type an unambiguous typo of a common word (e.g. 'wprld ') so autocorrect fires and the corrected word plus the next word-in-progress are both visible
- Capture right after the space commits the correction

> Want the corrected word visible in the field, ideally with the suggestion strip showing normal completions for the next word (not the correction itself, which is already committed).

### 84. `smart/autocorrect-skip-allcaps`

**Shot:** ALL-CAPS words pass through untouched next to a corrected lowercase one.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text --es theme dark
- Type an ALL-CAPS acronym (e.g. 'ASAP ') followed by a lowercase typo that does get corrected (e.g. 'wprld '), so both the untouched capital word and the corrected word are visible together
- Capture after both words have committed

> Illustrates the ALL-CAPS skip rule by contrast with a normal correction in the same field.

### 85. `smart/autocorrect-backspace-undo`

**Shot:** Backspace right after a correction restores what you originally typed.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text --es theme dark
- Type a word that autocorrects (e.g. 'wprld ') so it commits as 'world ', then immediately press backspace once without moving the cursor or typing anything else
- Capture right after the backspace, with the field showing the original typed word restored (e.g. 'wprld ')

> This is a discrete before/after state, not motion — a single still after the backspace is sufficient, no animation needed.

### 86. `smart/autofill-chips-strip`

**Shot:** Saved-login chips from a password manager, filling the suggestion strip on a login field.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind password
- Set a password manager (e.g. Google Password Manager or a test manager with saved entries for this package) as the system autofill service before capture, otherwise no chips will appear
- Focus the field so the IME requests inline suggestions and the manager returns at least 2-3 chips

> Needs a real autofill service active with saved credentials for the docs harness's package/domain, or the chip row will be empty. Chips are drawn by the manager itself, so exact visual varies by which manager is used for the shot -- pick one with a clean, generic chip style (avoid a branded one if possible).

### 87. `smart/autofill-dismiss`

**Shot:** Dismissing the chip row to type normally instead.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind password
- Same autofill-service setup as smart/autofill-chips-strip
- Focus the field to bring up chips, then frame the shot on the dismiss (X) button at the end of the chip row before tapping it

> Should show the chip row with the dismiss X button visible/highlighted, not the post-dismiss state (that's just the ordinary strip, not worth a separate shot).

### 88. `smart/calc-chip`

**Shot:** Typing 12*8 puts a calc chip on the suggestion strip.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Switch to WM Keyboard, focus the field, type 12*8, screenshot the strip before tapping the chip.

> Confirm Settings > Typing > Smart chips and Calculate as you type are both on (both default true). Chip fills the whole strip width.

### 89. `smart/currency-chip`

**Shot:** A currency chip: amount and source on the left, the converted amount on the right, gear button to open the full converter.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Type 150 usd. If rates haven't loaded yet the chip shows a brief spinner — wait a moment for it to resolve before shooting.
- Default currency pair is USD to BDT so the result should read as an amount in Taka.

> Needs network access once for open.er-api.com (or frankfurter.app fallback) to fetch rates the first time.

### 90. `smart/keyword-chip`

**Shot:** Typing wiki offers a narrow chip to open Wikipedia, leaving room for word candidates.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Type wiki (do not add a trailing space) and screenshot the strip — expect a narrow 'Open Wikipedia' chip beside word candidates, not filling the whole strip.

> Tool keywords toggle (Typing > Smart chips > Tool keywords) must be on; default true.

### 91. `smart/clipboard-entity-strip`

**Shot:** The Clipboard panel: dashed entity chips above the history, pulled out of several different clips.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Beforehand, copy 2-3 separate pieces of text into clipboard history containing distinct entity types, e.g. 'Your verification code is 483920', 'call me at 555-123-4567', and 'see https://example.com/docs' (as separate copy actions so they land as separate clips).
- Open the Clipboard tool from the toolbar or toolbox and screenshot the dashed-outline chip strip above the clip grid.

> Detect codes, numbers & links (Tools > Clipboard) must be on; default true. Up to 12 chips shown, sorted OTP, then PHONE, then URL.

### 92. `smart/otp-fresh-copy-chip`

**Shot:** Copying a verification text offers just the code, not the whole message, right on the suggestion strip.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Copy a message like 'Your verification code is 483920' from outside the harness (e.g. Messages or Notes) so it becomes the most recent clip.
- Switch to WM Keyboard and focus the field with no text typed yet; the idle suggestion strip should show a dashed CODE chip instead of the ordinary full-clip paste chip.

> Requires both Suggest recent copy and Detect codes, numbers & links on (Tools > Clipboard); both default true. This fast path is OTP-only — a phone number or URL copy shows the ordinary full-clip chip instead.

### 93. `smart/contact-name-suggestion`

**Shot:** A contact name completing in the suggestion strip, mid-word.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Enable Typing / Suggest contact names first (grants READ_CONTACTS) and ensure at least one seeded contact with a first+last name is present on the test device/emulator
- Type the first few letters of a seeded contact's first name (e.g. 'Was' for 'Wasi') so the completion appears mid-word in the suggestion strip
- Capture before committing the word so the strip shows the live completion

> Needs a test-device contact seeded ahead of capture (e.g. via adb shell content insert into raw_contacts/data, or the emulator's Contacts app) since this can't be captured against an empty address book. Capture agent should pick a name that clearly demonstrates completion (a distinctive first name, not a common dictionary word) so the shot isn't ambiguous with an ordinary dictionary suggestion.

### 94. `smart/grammar-panel-open`

**Shot:** The Grammar panel open above the key rows, with a spelling issue and its fix chips.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text --es text 'This dont work good and it are broken.'
- Switch to WM Keyboard (full flavor build — ENABLE_GRAMMAR must be true), focus the field, open the toolbox and tap Grammar check
- Wait for the immediate check to finish (checking spinner clears) before capturing

> Panel renders as a 146dp strip above the key rows, not full-bleed — capture with the key rows visible underneath. Needs a full-flavor build; lite has no Grammar tool at all.

### 95. `smart/grammar-toolbox-entry`

**Shot:** Grammar check in the toolbox, with the dialect chip and a live issue count.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open the toolbox grid (not the panel itself) and capture with the Grammar check tile visible
- Full flavor build required

> Shows the entry point in the toolbox grid, distinct from the panel-open shot. If default toolbox order is used, Grammar check appears after AI, not near the top — may need to scroll the toolbox grid into view.

### 96. `smart/grammar-fix-chips`

**Shot:** A card mid-fix: struck-through original text, fix chips, and Harper's explanation.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text --es text 'Their going to the store, but there not sure when.'
- Open Grammar check, wait for lints, zoom/crop to a single issue card showing struck-through text, arrow, and multiple fix chips

> Close-up crop of one GrammarLintCard; pick a lint with 2+ distinct suggestions so multiple fix chips are visible.

### 97. `smart/spell-checker-underline`

**Shot:** A misspelled word underlined by the system spell checker in another app, with a contact-name correction offered first.

- First select WM Keyboard (Harper) as the system spell checker via Android's own Spell checker picker (see the spell-checker-picker shot's setup)
- Grant the Contacts permission and turn on Suggest contact names under Typing settings so a real contact is available to demonstrate the correction-menu priority
- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Type a misspelled word (ideally close to the test contact's name) into the field and wait for the underline to appear, then trigger the correction menu

> The underline is drawn by Android's own text-service framework on any focused EditText, independent of which on-screen keyboard is active -- DocsShotActivity's own field works fine as the target even though the feature is conceptually 'any app'. Full-flavor build only (needs libharper_jni.so).

### 98. `smart/suggestion-strip-anatomy`

**Shot:** Word candidates in the suggestion strip, mid-word.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text --es theme dark
- Focus the field and type a partial word with several real completions (e.g. "cons") without pressing space, so three word candidates fill the strip.

> Used twice on the page (lead + anatomy section reuse the same shot id). Default settings are fine -- suggestions on, center-primary on.

### 99. `smart/suggestion-strip-emoji-tail`

**Shot:** Word candidates followed by predicted emoji.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text --es theme dark
- Type the whole word "birthday" (do not press space) -- emoji prediction is on by default and should append 🎂🎉🥳🎁 after the word candidates.

> Confirms the emoji tail rides after word chips, capped at 4 (shownEmojiSuggestions.take(4)).

### 100. `smart/suggestion-strip-ambiguous-correction`

**Shot:** A genuinely ambiguous typo offered as two tappable candidates instead of being auto-corrected.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text --es theme dark
- Type "helo" (do not press space) -- this typo is evenly split between "hello" and "help" per SuggestionEngine's edit-distance scoring, so autocorrect won't fire but both should appear as strip candidates.

> Verify at capture time that both words actually appear as top candidates on the shipped dictionary -- if the scoring has since shifted, substitute another word documented as ambiguous in autocorrect.mdx ("tesr" -> "test"/"tear") instead of changing the claim without re-checking source.

### 101. `smart/punctuation-chips`

**Shot:** Punctuation chips riding after the word candidates.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/typing
- Scroll to the Suggestions group and turn on "Punctuation suggestions" (off by default).
- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text --es theme dark
- Type an ordinary word with no strong emoji association, e.g. "hello" (do not press space), so word candidates show with the punctuation tail (. , ? ! ') instead of an emoji tail.

> Punctuation only shows when word candidates exist and no emoji prediction claimed the tail -- pick a word unlikely to trigger an emoji suggestion.

### 102. `smart/suggestion-strip-shift-recase`

**Shot:** Suggestion chips re-cased to Title Case while Shift is held.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text --es theme dark
- Type a lowercase word with candidates, e.g. "world" (do not press space).
- Press and hold the Shift key without releasing, and capture the strip while held -- chips should show Title Case.
- For the caps-lock (UPPERCASE) state, double-tap Shift to lock it instead of holding, then capture again if a second variant is wanted.

> This is a live, hold-contingent state (reverts the instant Shift is released), so capture must happen while physically holding the key -- coordinate with whatever capture harness can hold a keypress. A single still of the Title Case state is sufficient; not registering an anim sibling since the feature is a stable held-state, not continuous motion.

### 103. `tools/flashlight-toggled-on`

**Shot:** Flashlight is a direct toggle — no panel opens; the toolbox tile lights up while the torch is on.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode blank --es theme dark
- Focus the field, open the keyboard, open the toolbox (grid button on the toolbar), tap Flashlight to turn the torch on. Needs a device with a flash.

> Capture the toolbox tile itself lit with its accent color (#FFCA28), not a panel — Flashlight never opens one.

### 104. `tools/compass-panel-qibla`

**Shot:** The compass panel: rotating rose, degree readout, and the qibla marker pointing toward Mecca.

- Before shooting: under Settings → Tools → Weather, save a location (needed for the qibla bearing); under Settings → Tools → Compass, turn on 'Show qibla'.
- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode blank --es theme dark
- Focus the field, open the keyboard, open the toolbox, tap Compass. Orient the device so the rose and the 🕋 marker are both clearly visible.

> Without a saved location the panel shows 'Qibla needs a saved location (weather tool settings)' instead of the marker — set the location first.

### 105. `tools/level-panel`

**Shot:** Bubble level: a bullseye bubble plus a vertical and horizontal tube vial, with an angle readout below.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode blank --es theme dark
- Focus the field, open the keyboard, open the toolbox, tap Bubble level. Tilt the device slightly off-flat so the bubble and vials are visibly off-center rather than all accent-colored.

### 106. `tools/moon-phase-panel`

**Shot:** Moon phase: the current lit fraction, phase name, and next full/new moon dates — computed on-device.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode blank --es theme dark
- Focus the field, open the keyboard, open the toolbox, tap Moon phase.

> No network wait needed — this panel is pure arithmetic and renders instantly.

### 107. `tools/weather-panel-ready`

**Shot:** Weather: current conditions plus a stat grid — feels-like, humidity, wind, and whatever else Open-Meteo returns for that location.

- Before shooting: under Settings → Tools → Weather, search and save a location so the panel has data.
- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode blank --es theme dark
- Focus the field, open the keyboard, open the toolbox, tap Weather, wait for the fetch to finish (Ready state, not Loading).

> Prefer a location/time where Open-Meteo returns the optional stats (rain chance, cloud cover, pressure, UV, sunrise/sunset) so the full wrapping grid is visible.

### 108. `languages/avro-lenient-spelling`

**Shot:** Avro phonetic typing: "ami valo asi" commits as আমি ভালো আছি.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Switch the active keyboard layout to Avro phonetic (বাংলা · Bangla) if it isn't already active — spacebar swipe/hold-drag picker or the 🌐 key.
- Type "ami valo asi" and tap space so it commits, leaving আমি ভালো আছি in the field.
- Screenshot the field with the committed text (composing preview cleared).

> Avro is enabled by default on a fresh install but may not be the currently *active* layout — switch to it explicitly first.

### 109. `languages/probhat-key-grid`

**Shot:** প্রভাত (Probhat) active: Bengali letters typed directly, no phonetic conversion.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Switch the active layout to প্রভাত (Probhat) — spacebar hold-drag picker or 🌐 key cycle (it's enabled by default alongside Avro/Jatiya/QWERTY).
- Screenshot the idle keyboard's base (unshifted) letter grid — no need to type anything.

> Base layer is enough for the page; do not confuse with Jatiya, which is a different grid.

### 110. `typing/spacebar-cursor-swipe`

**Shot:** Dragging the spacebar moves the text cursor a character at a time.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind multiline
- In settings, set Typing / Gestures / Hold + swipe on spacebar to Cursor (it's the default)
- Drag the spacebar horizontally and hold mid-drag for the capture

> Show the cursor mid-field after a few character-steps of drag; a still frame is fine since the motion itself is generic left/right dragging.

### 111. `typing/backspace-word-swipe`

**Shot:** Dragging backspace to the left deletes text a word at a time instead of a character at a time.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind multiline --es text "the quick brown fox jumps"
- Confirm Typing / Backspace / Swipe to delete words is on (default)
- Press backspace and drag left past the first word's threshold, hold mid-gesture for the capture

> Capture mid-drag so a word or two is visibly gone but the finger is still down on backspace, if the harness can hold a synthetic touch.

### 112. `typing/volume-cursor-control`

**Shot:** With Volume cursor control on, the hardware volume keys move the text cursor instead of the volume.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind multiline
- Turn on Typing / Volume keys / Volume cursor control
- adb shell input keyevent KEYCODE_VOLUME_DOWN (moves cursor left) or KEYCODE_VOLUME_UP, then capture the field with the cursor visibly moved

> No dedicated on-screen UI changes for this feature — the shot just needs to show the text field with a moved cursor position; caption/context carries the explanation. Consider whether this shot is worth capturing at all vs. relying on the settings-screen shot alone; flagging for the capture-phase owner's judgement.

### 113. `tools/calculator-panel`

**Shot:** The Calculator panel: live expression and result, the scientific function row, and the custom digit keypad.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open the toolbar/toolbox and tap Calculator (or type its keyword 'calc' and tap the smart chip's gear icon).
- Type an expression like '12*(4+1)' so the live result and the Insert chip both show; leave the scientific row and the deg/rad chip visible in frame.

> Calculator is not pinned by default (default pinned row is Emoji/Clipboard/Settings) — reach it from the toolbox, or Settings > Tools > Calculator to pin it first. This shot doubles as the page's hero image, placed right after the lead paragraph.

### 114. `tools/unit-converter-panel`

**Shot:** The Unit converter panel: category chips, From/To unit rows, the swap icon, and the shared digit keypad.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open the toolbar/toolbox and tap Unit converter.
- Pick a recognizable category/pair (e.g. Length, feet to meters) and type a nonzero value so the converted result and Insert chip are visible.

> Reopens on whichever category/unit pair was used last (unitConvertLast setting), so a fresh install starts on category index 0 with its default From/To pair.

### 115. `tools/currency-converter-panel`

**Shot:** The Currency converter panel: a From/To currency pair, live converted amount, and the refresh icon.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Ensure the device has network access so exchange rates load (open.er-api.com, falling back to frankfurter.app).
- Open the toolbar/toolbox and tap Currency converter; type an amount so the converted result and Insert chip are visible.

> Default pair is USD to BDT. This is the one tool on the page that needs a live network fetch — capture after rates have loaded (CurrencyUi.Ready), not mid-spinner (CurrencyUi.Loading).

### 116. `tools/symbols-panel`

**Shot:** The Symbols panel: category chips over an adaptive grid, with a Recents chip once you've used a few.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open the toolbar/toolbox and tap Special symbols.
- Tap a couple of symbols first so a Recents chip appears at the front of the category row, then land on the Math or Greek category for the grid shot.

> Nine categories total: Fractions, Math, Greek, Arrows, Currency, Superscript, Punctuation, Shapes & stars, Misc.

### 117. `tools/qr-generator-panel`

**Shot:** The QR generator panel: editable content line, live QR preview, and Send as image.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text --es text 'https://wmkeyboard.app'
- Open the toolbar/toolbox and tap QR code generator — it seeds its editable buffer from the field text on open.
- Leave a short URL or short text in the buffer so the QR renders cleanly, with the Send as image chip visible.

> Unlike the other five tools on this page, the key rows stay visible underneath this panel — keystrokes reroute into the panel's own buffer instead of replacing the keys.

### 118. `tools/password-generator-panel`

**Shot:** The Password generator panel: generated result, entropy estimate, and the Password/Passphrase tabs.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open the toolbar/toolbox and tap Password generator.
- Capture the default Password tab (Length 16, Uppercase/Digits/Symbols on) with the generated string, entropy estimate, Copy/Insert chips, and regenerate icon all visible.

> A second crop of the Passphrase tab (toggle in the tool's own Options) could be a useful companion shot but isn't required.

### 119. `tools/typing-test-panel`

**Shot:** A typing test in progress: the prompt with per-character correctness coloring, above the live key rows.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open the toolbar/toolbox and tap Typing speed test, start a run (default: Timed, 30s), and type a few words so correct/incorrect/pending characters are all visible in one frame.

> The only tool on this page whose panel does not replace the key rows — the prompt is typed on the real keys while the panel scores it from above.

### 120. `tools/typing-test-results`

**Shot:** Typing test results: WPM, accuracy, consistency, and the Insert score chip.

- Continue directly from the tools/typing-test-panel session — let the timer run out (or finish the word/quote target) to reach the results screen.
- Capture with the WPM/accuracy/consistency figures and the Insert score chip all visible.

> Insert score commits text in the form '83 WPM · 96% accuracy (30s)'. A personal-best banner is a nice-to-have if this isn't the first run at that config, but not required.

### 121. `reference/import-file-confirm-dialog`

**Shot:** Opening a WM Keyboard file from a file manager: one confirm dialog, then the import.

- Push a sample file (e.g. a valid .wmtheme.json exported from the app) to the device, e.g. /sdcard/Download/sample.wmtheme.json
- adb shell am start -a android.intent.action.VIEW -d file:///sdcard/Download/sample.wmtheme.json -t application/json -n com.wasimaster.wmkeyboard/.app.ImportFileActivity
- If the VIEW intent doesn't route correctly from adb (content:// URIs from a real file manager work more reliably than file:// from adb), instead open a file manager app on-device, navigate to the pushed file, and tap it — WM Keyboard's file association (AndroidManifest.xml intent-filter, pathPattern match on the compound extension) opens ImportFileActivity directly.

> This is ImportFileActivity's ImportFileDialog composable (app/src/main/java/com/wasimaster/wmkeyboard/app/FileImport.kt), a single AlertDialog over whatever was on screen — not a NavHost settings screen and not a DocsShotActivity harness mode, so host is approximated as 'settings' for lack of a better bucket; treat the setup steps as authoritative. Title/body come from WMFileTypes.identify()'s result (e.g. 'Import theme "<name>"?' for a theme). Using a .wmlayout.json with a deliberate minor error would additionally show the 'Changed on the way in' repairs list, which is a more interesting shot if the capture agent wants to show that branch instead.

### 122. `reference/shortcuts-cheat-sheet`

**Shot:** The cheat sheet after pressing the shortcut key twice, then ?, showing every bound letter.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Focus the field to bring up the keyboard
- Send a physical double-tap of Ctrl (or the two hardware KEYCODE_CTRL_LEFT down/up event pairs within 400ms) to arm the tool picker
- Send a KEYCODE_SLASH (or shifted '/') key event to expand the picker into the full cheat sheet
- Capture while the cheat-sheet overlay is showing

> Requires a physical/emulated hardware keyboard sending real KeyEvents (adb `input keyevent` against KEYCODE_CTRL_LEFT twice within the 400ms double-tap window, then KEYCODE_SLASH) since this is driven by WMKeyboardService.onKeyDown/onKeyUp, not touch. If DocsShotActivity/adb input cannot easily fake the double-tap timing, a fallback is to trigger it from a Bluetooth/USB test keyboard on the device.

### 123. `reference/troubleshooting-panel-captures-input`

**Shot:** An open tool panel capturing input — the toolbox button has become a back arrow.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.DocsShotActivity --es mode field --es kind text
- Open WM Keyboard on the field, open the toolbox, and open a genuine input-capturing panel tool (e.g. Search or Snippets — not a sensor/generator tool like Calculator, which replaces the key rows instead per tools/overview.mdx)
- Screenshot with the panel open, confirming the toolbox button area now shows a back arrow instead of the grid icon

> Purpose is to show the visual cue (back arrow) that a panel has captured the keyboard, referenced from tools/overview.mdx#panels-and-captured-keys. Pick whichever panel tool is easiest to reach and has an uncluttered look.

### 124. `themes/file-import-confirm`

**Shot:** Opening a theme file from a file manager shows a one-tap import confirmation.

- Place a sample .wmtheme.json file on-device (e.g. via adb push to Downloads).
- Open a file manager app and tap the file, or use: am start -a android.intent.action.VIEW -d file://<path>/Sample.wmtheme.json -t application/json
- Screenshot the resulting 'Import into WM Keyboard' confirm dialog (FileImport.kt ImportFileActivity).

> Dialog title is literally "Import {theme.name}?" with body "The theme is added to your themes and applied to the keyboard." and a single Import button (FileImport.kt:361-375). Activity is excludeFromRecents and translucent, so capture may need a real file-manager tap sequence rather than the DocsShotActivity harness.

---

## Manual navigation (see per-shot setup)  (54 shots)

```bash
python3 docs/screenshots/import.py manual
```

### 1. `accessibility/gestures-service-required`

**Shot:** The extra row that appears when TalkBack support is set to Gestures, before the service is granted.

- Navigate to settings root → Accessibility.
- Set "TalkBack support" to "Gestures" to reveal the "Keyboard gestures service required" row (only shows in this mode).
- Do NOT actually grant the accessibility service for this shot — capture the ungranted state.

> NavRow only rendered when settings.screenReaderMode == PASSTHROUGH, AccessibilityScreens.kt:172-192.

### 2. `start/editions-about-version`

**Shot:** Settings → About → Version names the flavor, build type, and version code.

- "about" is not in the deep-link allowlist (only typing/appearance/themes/languages/tools/search are), so open it by navigation rather than a wmkeyboard:// URI
- Open the WM Keyboard settings app, tap "About" in the About section on the settings home screen
- Screenshot the Version row under the App group — reads "Full build (release) · code …" or "Lite build (release) · code …"

> Capturing on a Full-flavor build is the more informative choice (shows "Full build" explicitly); a Lite capture would be an equally valid substitute if that's the build already installed for other shots in this batch.

### 3. `start/faq-privacy-summary`

**Shot:** The Privacy screen's on-device summary, with the data-clearing controls below it.

- Launch the app normally (am start -n com.wasimaster.wmkeyboard/.app.MainActivity) — the "privacy" route is NOT in the wmkeyboard://settings/ deep-link allowlist (SettingsShortcuts.kt Routes = typing, appearance, themes, languages, tools, search only), so this cannot be reached by deep link.
- On the home screen, tap 'Privacy' under the 'Data' group.
- Scroll to the bottom to frame the 'Your data' group (Clear learned words button) together with the closing caption text.

> The in-app caption at the bottom of this screen (MainActivity.kt ~8498-8503) currently only mentions the weather tool as the network exception; it's stale relative to the real tool list (dictionary, translate, GIF/sticker, search, AI). Known in-app content bug, not something for docs to fix, but don't let a future screenshot caption imply the in-app text is exhaustive.

### 4. `start/migrating-personal-dictionary`

**Shot:** The personal dictionary screen — words you add show 'Added by you'; words picked up from typing show a use count. There's no bulk-import button here.

- Route 'dictionary' is NOT in the SettingsShortcuts deep-link allowlist (only typing/appearance/themes/languages/tools/search are) — launch the app and tap Typing → Personal dictionary manually
- Add one word via the Add word button, and let another word accumulate a few uses from typing first, so both 'Added by you' and 'Seen N×' labels are visible

> count >= 200 shows 'Added by you' (MainActivity.kt:4438, UserLexicon.addWord boost=200); otherwise 'Seen N×'. Demonstrates there is no bulk-import UI on this screen, backing the page's 'no one-click import' claim.

### 5. `start/wizard-gestures-page`

**Shot:** The Spacebar & keys page — one choice sets both swipe directions at once.

- Force the onboarding wizard to show: clear app data (`adb shell pm clear com.wasimaster.wmkeyboard`) or otherwise reset the `onboardingDone` DataStore flag to false
- am start -n com.wasimaster.wmkeyboard/.app.MainActivity
- Tap through the wizard (Welcome → Your languages → Look & feel → [Emoji font, if shown] → Typing feedback) to reach the 'Spacebar & keys' page — not deep-link addressable, the onboarding route isn't in the settings URI allowlist
- Capture with the 'Cursor + language (recommended)' or default-selected radio option visible, plus the emoji-key and number-row toggles below it

> Onboarding page order is fixed but two pages are conditional (Emoji font only if the device's emoji font can't render the full catalog; Tool setup only if Calendar/Weather/Compass is enabled) — tap count to reach Gestures may vary by device/config. Clearing app data also resets enabled languages/tools back to factory defaults, which is fine for this shot.

### 6. `languages/layout-editor-grid`

**Shot:** The visual layout editor, showing the Letters layer with a key selected.

- From Key layouts, tap the edit icon on any enabled layout (e.g. the built-in QWERTY row) to reach keymap_edit/{layoutId}
- Tap one letter key to show the RowActionBar/RowHeightRow selected state

> Show the layer tabs (LayerChips) across the top and the 'Saved automatically' caption near the undo/redo icons.

### 7. `languages/key-edit-sheet`

**Shot:** The key edit sheet — label, output, action, width, and long-press alternates.

- From the layout editor grid, tap a text key (e.g. a letter) to open the KeyEditSheet bottom sheet
- Scroll to show Long-press alternates and the AlternatePreview chips if possible

> Pick a key with a few long-press alternates already set (e.g. a vowel from a shipped layout) so the alternate preview chips are visible.

### 8. `languages/layout-json-editor`

**Shot:** The raw JSON editor, with syntax highlighting and an Apply button.

- From the layout editor grid, tap 'Edit as JSON' near the bottom of the settings list to reach keymap_json/{layoutId}

> Capture with the default (unedited) JSON so syntax highlighting colors are visible without needing to type anything.

### 9. `languages/globe-key-toggle`

**Shot:** Layout & size → Bottom-row keys: turning off Emoji key instead of 🌐 restores the language-switch key.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/layout
- Scroll to the Bottom-row keys group at the bottom of the screen.

> globeAsEmoji defaults to true, so on a fresh install this toggle is ON and the bottom row shows an emoji key rather than 🌐 — capture the group with the toggle visible (either state is fine, but on-state matches the default and is the more useful shot).

### 10. `typing/size-position-per-screen`

**Shot:** Per-screen sizing: Landscape expanded, showing the Keyboard scale slider.

- Home → Keyboard group → Layout & size
- Scroll to 'Per-screen sizing' and tap the 'Landscape' row to expand its editor (Keyboard scale, Key height, Bottom padding, Keyboard width, Font size sliders)

> Group at MainActivity.kt:3590-3695; expanded editor renders under whichever ScreenVariant row is tapped.

### 11. `smart/personal-dictionary-clear`

**Shot:** The 'Your data' group on the Privacy screen, with the one-tap clear button.

- Privacy is not deep-link allowlisted -- open Settings home and tap 'Privacy'
- Scroll to the bottom 'Your data' group so the 'Clear learned words' button is in frame

> Do not actually tap Clear learned words during capture unless the shot specifically needs the confirmation state -- there is no confirmation dialog, it deletes immediately.

### 12. `smart/personal-dictionary-backup-toggle`

**Shot:** The Dictionary section of a manual export, off by default.

- Backup & restore is not deep-link allowlisted -- open Settings home and tap 'Backup & restore'
- Scroll to the 'Include in export' group so the 'Dictionary' toggle (off) is in frame

> Shoot with the toggle in its default off state to match the caption; no need to actually export.

### 13. `smart/spell-checker-picker`

**Shot:** Android's own Spell checker picker, with WM Keyboard (Harper) listed as an option.

- am start -n com.android.settings/.Settings\$SpellCheckersSettingsActivity
- If that component doesn't resolve on this OS image, fall back to: am start -a android.settings.INPUT_METHOD_SETTINGS, then open Spell checker from there (mirrors the app's own fallback in MainActivity.kt's openSpellCheckerSettings())

> This is Android's own system Settings screen, not WM Keyboard's app settings. Entry shows as 'WM Keyboard (Harper)' with one 'English' subtype (spell_checker_name / spell_checker_subtype_english strings in app/src/full/res/values/strings.xml). Only present on full-flavor installs.

### 14. `development/diagnostics-screen`

**Shot:** Settings → About → Diagnostics: filterable app log, crash records, and a one-tap report export.

- Not deep-link allowlisted (only typing/appearance/themes/languages/tools/search are) — navigate by hand: open the app, tap into Settings, tap About, tap Diagnostics.
- Type a few characters into any field first, or trigger a tool error, so the App log group isn't empty for the shot.
- Leave 'Include the system log' toggled off (its real default) unless specifically demonstrating that toggle.

> Backed by core/common/.../debug/DebugLog.kt (CAPACITY=500 ring buffer, CRASH_CAPACITY=10) and app/.../DebugLogScreen.kt. Route is composable("debug_log") off MainActivity.kt's about screen, not itself deep-link allowlisted. Also relevant to reference/settings/about.mdx (different page, different agent) — this entry is independent, capture once and it can serve either page if convenient, but each page's manifest is authoritative for its own placeholder id.

### 15. `reference/file-import-dialog`

**Shot:** The one-time confirm dialog shown before any file import goes through.

- Push a sample export (e.g. a small *.wmtheme.json) to the device's Downloads folder via adb push.
- Open a file manager app, navigate to Downloads, and tap the file — this launches ImportFileActivity's confirm dialog (a separate activity, not reachable via a settings deep link).
- Capture the dialog before tapping Import.

> Any recognized extension works for a generic 'here's the dialog' shot; a theme import (title 'Import <name>?') is a reasonable representative choice since its wording is short.

### 16. `reference/plugin-import-gate`

**Shot:** Opening a plugin file while the Plugins subsystem is off — nothing installs or runs.

- Ensure Tools > Plugins > Allow plugins is OFF (the default — no action needed on a fresh install).
- Push a sample .wmplugin file to Downloads via adb push, open it from a file manager the same way as the file-import-dialog entry.
- Capture the 'Plugins are turned off' dialog.

> Distinct from the generic file-import-dialog shot: this specific dialog never shows the plugin's name, only the 'turn plugins on in Settings > Tools > Plugins first' message. Requires the subsystem toggle to be off, which is the default state.

### 17. `reference/troubleshooting-diagnostics-screen`

**Shot:** The Diagnostics screen: report actions at the top, the filterable app log below.

- Diagnostics is NOT in the wmkeyboard://settings/ deep-link allowlist (only typing, appearance, themes, languages, tools, search) — navigate by hand instead: open the app, go to About, tap Diagnostics
- Capture with the Report group (Share diagnostics / Copy to clipboard / Include the system log) visible plus at least a few App log entries below; the Crashes section only appears if a crash exists so it's fine if it's absent

> Route is debug_log (SettingsScreen("Diagnostics", ...) in MainActivity.kt:728-732), reached only via About, confirmed not on the deep-link allowlist in reference/deep-links.mdx.

### 18. `reference/settings/about-screen`

**Shot:** The About screen: Share, App, Documentation, Third party, and Word lists groups.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.MainActivity (launches straight to the Settings home screen)
- 'about' is not in the wmkeyboard://settings/<route> deep-link allowlist (only typing, appearance, themes, languages, tools, search are) — tap the 'About' row inside the standalone 'About' group near the bottom of the home screen instead
- capture the resulting About screen, scrolled to show at least the Share and App groups

> Home screen has a dedicated SettingsGroup("About") with a single HomeItem that navigates to route "about" (MainActivity.kt ~line 872). Full scroll shows five groups: Share, App, Documentation, Third party, Word lists.

### 19. `reference/settings/licenses-screen`

**Shot:** The open-source licences list, grouped by bundled components, downloadable data packs, and online services.

- From the About screen (see about-screen entry), tap 'Open-source licences' under the 'Third party' group
- capture the top of the list showing the 'Bundled in the app' group header and its first several rows

> A full-flavor build shows 19 bundled entries (12 always-present + 7 gated behind ENABLE_GRAMMAR/ENABLE_ML_KIT_*/ENABLE_LOCAL_LLM/ENABLE_WHISPER in app/build.gradle.kts); lite shows 12. Capture from a full build so all three groups (bundled/data packs/services) and the flavor-gated rows are visible if scrolled.

### 20. `reference/settings/diagnostics-screen`

**Shot:** Diagnostics: report actions, the app log with its filter and level chips, and Clear app log at the bottom.

- From the About screen (see about-screen entry), tap 'Diagnostics' under the 'App' group
- reproduce at least one logged event beforehand (open any panel) so the App log list isn't empty for the shot
- capture the top of the Diagnostics screen: Report group (Share diagnostics / Copy to clipboard / Include the system log toggle) through the App log filter and level chips

> Route is "debug_log", also not deep-link allowlisted. Crashes group only renders if core/common's DebugLog crash file is non-empty — leave it out of frame unless deliberately capturing that state, in which case use a separate entry.

### 21. `reference/settings/accessibility-color-vision-picker`

**Shot:** Colour vision set to Deutan, with the five filter options shown in the picker.

- Navigate to the Accessibility screen (no direct deep link; via Settings home > Accessibility)
- Tap the Colour vision row to open its choice picker and select Deutan

> Picker options in order: Off, Deutan, Protan, Tritan, Grey (on-screen label is 'Grey', not 'Grayscale').

### 22. `reference/settings/accessibility-screen-reader-group`

**Shot:** TalkBack support set to Gestures, with the Keyboard gestures service row asking to be granted.

- Navigate to the Accessibility screen (via Settings home > Accessibility)
- Set TalkBack support to Gestures so the conditional Keyboard gestures service row appears
- Capture before granting the 'WM Keyboard gestures' accessibility service so the row reads 'Keyboard gestures service required'

> Same subject as accessibility/gestures-service-required from the finished guide page; distinct id/path per this page's manifest namespace.

### 23. `reference/settings/accessibility-touch-group`

**Shot:** The Touch group: ignore repeated presses and long-press delay.

- Navigate to the Accessibility screen (via Settings home > Accessibility)
- Scroll to the bottom Touch group and screenshot Ignore repeated presses, Long-press delay, Key size & spacing, and Haptics & sound rows

> Same subject as accessibility/touch-group from the finished guide page; distinct id/path per this page's manifest namespace.

### 24. `reference/settings/sticker-packs-list`

**Shot:** Sticker packs: your own packs, plus New pack and Import a pack.

- Open Settings home
- Tap the search icon and search "Sticker packs"
- Tap the result (breadcrumb Tools › Stickers)

> Route is sticker_packs, not deep-link allowlisted. Create one or two packs with a handful of stickers each before capturing so the thumbnail strips and "N sticker(s)" subtitles are visible rather than an empty list.

### 25. `reference/settings/plugins-screen`

**Shot:** Plugins: the Allow plugins switch and what a plugin can and can't do.

- Open Settings home
- Tap the search icon and search "Manage plugins"
- Tap the result (breadcrumb Tools › Plugins)

> Route is plugins, not deep-link allowlisted. The 'What a plugin can and can't do' static group shows regardless of switch state; turn Allow plugins on first if the shot should also include the Installed/Add groups.

### 26. `reference/settings/backup-import-confirm`

**Shot:** The import confirmation dialog, listing what a backup file contains before anything is applied.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.MainActivity
- No deep link for 'backup' route (not in the allowlist) — navigate via Data > Backup & restore.
- Push a sample .wmconfig.json backup file to the device first (adb push), or export one from the app, then tap 'Import backup' and pick that file to trigger the 'Import backup?' AlertDialog.
- Capture while the AlertDialog is showing, before tapping Import or Cancel.

> Dialog text is built dynamically per section found in the file (e.g. 'Themes: 3 custom themes') plus a fixed merge/replace sentence and an optional API-keys warning line — pick a sample file with at least 2-3 sections and, ideally, one containing API keys so the warning line is visible too.

### 27. `reference/settings/emoji-overview`

**Shot:** The Emoji settings screen, showing Access and Suggestions at the top.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/emoji
- Screen opens scrolled to the top — Access and Suggestions groups visible.

> Top of the Emoji settings screen (route emoji, deep-link allowlisted).

### 28. `reference/settings/emoji-panel-group`

**Shot:** The Emoji panel group, with the Emoji keywords row at the bottom.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/emoji
- Scroll to the 'Emoji panel' group (Return to keyboard after inserting ... Emoji keywords NavRow).

> Scroll-position shot; no deep link to the exact scroll offset, tap-scroll needed after opening route emoji.

### 29. `reference/settings/emoji-row-group`

**Shot:** The Emoji row group with Own row selected.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/emoji
- Scroll to 'Emoji row' group
- Set Emoji row = Own row so the content/count/scroll rows and the Rows & bars caption are all visible

> Set Emoji row to 'Own row' before the shot so the conditional rows (content, count, scroll) and the caption about Rows & bars all render.

### 30. `reference/settings/emoji-style-group`

**Shot:** The Emoji style group, with the live font preview row and font choices.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/emoji
- Scroll to the bottom 'Emoji style' group

> Shows the live 8-emoji preview row plus the Emoji font choice and Hide emoji this phone can't display toggle.

### 31. `reference/settings/emoji-keywords-screen`

**Shot:** The Emoji keywords screen, with per-language download and import controls.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/emoji
- Tap the 'Emoji keywords' row in the Emoji panel group (route emojikeywords is not itself deep-link allowlisted, reach it by tap)

> Sub-screen reached only by tapping the NavRow from the Emoji settings screen — emojikeywords is not on the deep-link allowlist.

### 32. `reference/settings/fonts-english-section`

**Shot:** The Keyboard font screen: English font section with Google Fonts, each row previewed in its own face.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/fonts
- scroll to top so the English font section's curated Google Fonts list is visible

> Route 'fonts' is allowlisted for deep-link (verified in MainActivity.kt NavHost). This can reuse/match the themes/font-picker-english shot already registered by the themes/fonts.mdx page if the capture team wants to dedupe — same screen, same section.

### 33. `reference/settings/key-layouts-list`

**Shot:** The Key layouts screen: your own layouts, an import row, and the enabled built-ins.

- am start -n com.wasimaster.wmkeyboard/.app.MainActivity
- Tap 'Key layouts' under the Keyboard group on Settings home ('keymaps' is not on the wmkeyboard://settings/ deep-link allowlist, so this needs a tap sequence, not a deep link).

> Fresh install shows QWERTY + Avro + Probhat + Jatiya under Built in, and empty 'Your layouts'. For a more interesting shot, add/edit one custom layout first so 'Your layouts' isn't showing its empty state.

### 34. `reference/settings/key-layouts-delete-confirm`

**Shot:** Confirming a reset on an edited built-in layout.

- From the Key layouts list, edit a Built in layout (e.g. QWERTY) so it carries an override.
- Back out to the Key layouts list, then tap the Reset icon on that row.

> Must edit a shipped layout first, otherwise its row has no Delete/Reset button at all.

### 35. `reference/settings/layout-editor-grid`

**Shot:** The visual layout editor: the Letters layer, a key selected, and its row action bar.

- From the Key layouts list, tap any enabled layout to open Edit layout.
- Tap a key in the grid so the row action bar and row height slider appear before capturing.

> Capture on the Letters layer with a key selected so the row action bar (Row N, add/duplicate/delete row) and row height slider are both visible.

### 36. `reference/settings/key-edit-sheet`

**Shot:** The key edit sheet: label, output, action, width, and long-press alternates.

- From Edit layout, tap any Text-action key to open its bottom sheet.
- Scroll the sheet so Label/Output/Action/Width and the long-press alternates field are all in frame.

> Pick a key with the 'Types text' action so the icon/icon-hint/alternates/field-adaptation fields (text-action-only) are shown, not just the universal fields.

### 37. `reference/settings/layout-json`

**Shot:** The Layout JSON screen: the stored layout as text, with syntax highlighting.

- From Edit layout, tap 'Edit as JSON' to open Layout JSON.
- Scroll the text field so real syntax-highlighted content is visible, not just the top of an empty-looking field.

> Capture on a layout with a nontrivial layer (a few rows/keys) so the JSON isn't a near-empty document.

### 38. `reference/settings/keypress-overview`

**Shot:** The top of the Key press screen: the Haptic feedback group with its style chips.

- am start -n com.wasimaster.wmkeyboard/.app.MainActivity
- Tap the "Key press" tile on the settings home grid (route "keypress" is not in the wmkeyboard://settings/ deep-link allowlist — only typing, appearance, themes, languages, tools, search are — so there is no am start -d shortcut).

> Top of screen, no scroll needed: Key press haptics toggle + Haptic style chip row visible without scrolling on a standard phone height.

### 39. `reference/settings/keypress-haptic-custom-sliders`

**Shot:** Haptic style set to Custom, showing the strength and intensity sliders.

- am start -n com.wasimaster.wmkeyboard/.app.MainActivity
- Tap "Key press", then tap the "Custom" chip under Haptic style to reveal the Haptic strength and Haptic intensity sliders.

> Same underlying UI state as typing/haptic-style-custom-sliders on the /typing/feedback/ guide page, but scoped/named separately per this page's own manifest namespace — capture once and it may be reusable, otherwise capture the same state twice.

### 40. `reference/settings/keypress-popup-timing`

**Shot:** Key popup and Timing groups, mid-screen.

- am start -n com.wasimaster.wmkeyboard/.app.MainActivity
- Tap "Key press", scroll to where the Key popup group's sliders (Popup font size, Popup height) meet the Timing group's sliders (Long-press delay, Key repeat interval, Caps-lock double-tap).

> Pick a scroll position that shows the boundary between the two groups so both headers are visible.

### 41. `reference/settings/keypress-long-press-shortcuts`

**Shot:** Long-press shortcuts: the currency keys field and the Hold-letter toggles.

- am start -n com.wasimaster.wmkeyboard/.app.MainActivity
- Tap "Key press", scroll to the bottom Long-press shortcuts group so the Currency keys text field and at least a few of the Hold A/C/X/V/Z/Y toggles are visible together.

> Currency keys field should show its backfilled default (৳ € £ ¥ ₹ ₿) since it has never been edited on a fresh install.

### 42. `reference/settings/layout-overview`

**Shot:** The Layout & size screen, opening on the Number row and Numerals groups.

- Not deep-link allowlisted (SettingsShortcuts.Routes = typing, appearance, themes, languages, tools, search only) — navigate manually: launch the app, tap the 'Layout & size' tile on the home screen (AspectRatio icon).

> Capture with default (off) state — Number row off, so only the toggle and the Numerals group's Type native digits in row show below it.

### 43. `reference/settings/layout-per-screen-editor`

**Shot:** A Per-screen sizing row expanded, with Keyboard scale and the other override sliders.

- From the Layout & size screen (see layout-overview setup), scroll to 'Per-screen sizing' and tap the 'Landscape' row to expand its inline editor.

> Shows Keyboard scale, Key height, Bottom padding, Keyboard width, Font size sliders plus the 'Follow portrait again' row once any field is touched.

### 44. `reference/settings/layout-one-handed-split-floating`

**Shot:** One-handed mode set to Right, with the Portrait width, height, and side sliders below it.

- From the Layout & size screen, scroll to 'One-handed, split & floating' and set One-handed mode to Right so the Portrait/Landscape sub-sliders render.

> Default is One-handed mode = Off, which hides the per-orientation sliders — set it to Right for the capture so the full group is visible.

### 45. `reference/settings/layout-bottom-row-keys`

**Shot:** The Bottom-row keys group: Comma key opens emoji and Emoji key instead of the globe key.

- From the Layout & size screen, scroll to the bottom 'Bottom-row keys' group.

> Default state: Comma key opens emoji = Off, Emoji key instead of globe = On.

### 46. `reference/settings/modes-list`

**Shot:** Keyboard modes: the six default modes, each with its auto-activation summary, plus New mode.

- 'modes' is NOT in the wmkeyboard://settings/ deep-link allowlist (only typing, appearance, themes, languages, tools, search are) — no direct deep link works here
- Open WM Keyboard settings, tap the Keyboard group's 'Keyboard modes' row (subtitle 'Per-app setups: email, browser, coding, passwords')
- Capture on a fresh/default install so all six seeded modes are visible with their default binding summaries

> Row order is settings.keyboardModes as stored (seed order: Passwords, Email, Browser, Chat, Writing, Coding), each showing its icon + modeBindingsSummary (e.g. 'Auto: text fields', 'Auto: 6 apps + text fields', 'Auto: password fields'). New mode row has a plain Add icon at the bottom.

### 47. `reference/settings/mode-editor-top`

**Shot:** Edit mode: name, icon, and the What this mode changes group.

- From Keyboard modes, tap a built-in mode, e.g. Chat
- Screenshot the top of the editor: Name field, Icon row, then Emoji row / Symbol row / Theme choices at the top of 'What this mode changes'

> Chat ships with emojiBarMode ALWAYS and no themeId set, so Theme should show 'Inherit — whatever theme is set' with no Clear button in this shot.

### 48. `reference/settings/mode-editor-custom-tools`

**Shot:** A mode's Custom pinned tools turned on, with its own tool chips and reorder option.

- From Keyboard modes, tap Chat (ships with toolbarTools already set, toolbarToolsAppend = true)
- Scroll to the Custom pinned tools toggle (already on) and capture the revealed Pinned tools behaviour choice ('Add to mine'), the tool chip picker, and Modify pinned tool order

> No extra taps needed to reveal this section on Chat since it's a built-in with the toggle already on; the four selected chips should be Emoji, GIF, Sticker, Voice.

### 49. `reference/settings/mode-editor-activation`

**Shot:** Field-type chips and the bound app list at the bottom of the mode editor.

- From Keyboard modes, tap Chat (has both apps and fieldKinds set, so it also shows the 'Both have to match' caption)
- Scroll to 'Activate automatically for': the Text field-type chip selected, the caption, and the bound chat-app list (Messenger, Instagram, Telegram, WhatsApp, Signal, Discord) with the Add app row below

> Chat is the only default mode with both apps and field types set, making it the best example of the AND-match caption text.

### 50. `reference/settings/privacy-screen-groups`

**Shot:** The Privacy screen: On-device learning, Android backup, and Your data groups.

- Privacy is NOT in the wmkeyboard://settings/<route> deep-link allowlist (SettingsShortcuts.kt only allows typing, appearance, themes, languages, tools, search) — cannot use the am start -d wmkeyboard://settings/... shortcut.
- Launch com.wasimaster.wmkeyboard/.app.MainActivity normally, then tap through: Settings home -> 'Privacy' tile under the 'Data' group.
- theme=dark (default)

> Full-screen capture of all three SettingsGroups (On-device learning / Android backup / Your data) stacked; may need a scroll or a shortened device chrome to fit all three plus the footer caption in one shot — if it doesn't fit, prioritize showing all group headers over the footer text.

### 51. `reference/settings/privacy-backup-info-dialog`

**Shot:** The info dialog for Back up with Android, listing what's included and excluded.

- Navigate to Settings -> Privacy (same tap path as privacy-screen-groups; no deep link available).
- Tap the (i) info button next to 'Back up with Android' to open its explanation dialog (BACKUP_INFO text, MainActivity.kt:8403-8413).

> The dialog text is long — make sure the sentence 'Note that this does include saved API keys' is visible/legible in the crop, it's the one detail the page calls out as worth knowing.

### 52. `reference/settings/privacy-clear-learned-words`

**Shot:** Clear learned words, the only control in the Your data group.

- Navigate to Settings -> Privacy (same tap path as privacy-screen-groups; no deep link available).
- Scroll to the bottom 'Your data' group showing the standalone 'Clear learned words' OutlinedButton.

> No dialog appears on tap — do not capture mid-tap, just the resting button in its group.

### 53. `reference/settings/rows-overview`

**Shot:** The Rows & bars screen: Symbol row, Row order, and Symbol sets groups.

- Navigate: Settings home -> Keyboard group -> "Rows & bars" row (no deep-link route allowlisted for this screen; 'rows' is not in SettingsShortcuts.Routes, so open via in-app navigation)

> Full-screen capture showing all three groups if they fit; scroll to top first.

### 54. `reference/settings/rows-symbol-sets`

**Shot:** The Symbol sets group, with the edit icon open on Coding.

- Navigate to Rows & bars (see rows-overview setup)
- Scroll to the Symbol sets group
- Tap the edit (pencil) icon on the Coding row to show Edit symbol set open, or capture the list row with the icon visible if a two-shot composite is preferred

> Caption implies the edit screen is open on Coding — capture either the Symbol sets list with Coding's edit icon highlighted, or the Edit symbol set screen itself pre-filled with Coding's name/characters. Pick whichever the capture agent finds clearer; both are defensible readings of the caption.

---

## Settings › Languages  (19 shots)

```bash
python3 docs/screenshots/import.py settings-languages
```

### 1. `languages/key-layouts-gallery`

**Shot:** The Key layouts screen: your own layouts, the shipped set, and an import row.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages
- Tap into a language and enable at least one custom-eligible layout, or navigate Settings > Key layouts directly if a deep link for 'keymaps' is not allowlisted

> Route is 'keymaps' (title 'Key layouts'); not confirmed on the deep-link allowlist (only typing/appearance/themes/languages/tools/search are). Fall back to a tap-sequence from Settings home if wmkeyboard://settings/keymaps 404s. Shows two groups: 'Your layouts' (only appears once at least one custom/edited layout is enabled) and 'Built in'.

### 2. `languages/dictionary-download-row`

**Shot:** A language's Dictionary section: the built-in note, a size tier, and the Download button.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages
- Tap a language that has no bundled dictionary but does have a downloadable wordlist (e.g. French) to open its language screen, then screenshot the Dictionary section showing the size-tier dropdown and Download button before tapping it.

> wmkeyboard://settings/languages is on the deep-link allowlist per MainActivity.kt, but the per-language screen itself is reached by an in-app tap, not a deeper deep link — navigate manually from there. Pick a language whose full wordlist is bigger than the Small tier (50k words) so the size picker is actually visible.

### 3. `languages/dictionary-download-progress`

**Shot:** A dictionary mid-download, with the indeterminate progress bar and cancel action.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages
- Open the same language's Dictionary section, tap Download, and screenshot while the row shows the indeterminate LinearProgressIndicator and the close (cancel) icon, before it finishes.

> Timing-sensitive — needs the screenshot taken during the Downloading state, which for a small-to-medium list may only last a couple seconds. A larger language (e.g. Thai or Arabic with Large tier) gives more time to capture it.

### 4. `languages/fancy-text-layout-toggles`

**Shot:** The Fancy Text language screen: 22 style toggles under Layouts.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages
- Tap "Add language", search "fancy", tap the 𝓕𝓪𝓷𝓬𝔂 · Fancy Text row (this auto-enables Bold and opens this detail screen)
- Optionally toggle on a couple more styles (e.g. Small Caps, Double-struck) so the Layouts list shows a mix of on/off states, then scroll so the Layouts group is in frame

> Screen title renders in the fancy font itself (the language's own displayName). The Layouts group has 22 rows total, so a partial scroll position that shows several toggled + several untoggled reads best.

### 5. `languages/notation-add-language-search`

**Shot:** Add language: searching "morse" finds Morse code in the full catalogue.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages
- Tap 'Add language', type 'morse' into the search box, capture the filtered result row.

> Deep link lands on the Languages list screen; 'Add language' itself needs a manual tap since add_language isn't in the deep-link allowlist.

### 6. `languages/list-overview`

**Shot:** The Languages screen: your enabled languages, plus Add language to search the full catalogue.

- adb shell am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages

> Top of the Languages screen: 'Your languages' group (English/QWERTY + the three Bangla layouts enabled by default) ending in the 'Add language' row. No scrolling needed.

### 7. `languages/add-language-search`

**Shot:** Add language: search matches endonym, English name, id, and locale.

- adb shell am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages
- Tap 'Add language' at the bottom of the Your languages group

> Show a handful of matched rows with the search box populated (e.g. type 'ban' to surface Bangla/Bengali-adjacent entries), including at least one row marked 'Added' to show the state distinction.

### 8. `languages/language-detail-screen`

**Shot:** A language's own screen: layout toggles, numeral system, and other enabled languages to suggest from.

- adb shell am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages
- Tap a language with more than one layout, e.g. Bengali (Avro/Probhat/Jatiya), with at least one other language already enabled so 'Also suggest from' is visible

> Frame the top of the screen: Layouts group, Numerals group, and the start of Also suggest from. Do not scroll to Dictionary/Emoji — those are captured separately for dictionaries.mdx.

### 9. `languages/list-per-app-and-switcher`

**Shot:** Further down the Languages screen: per-app language memory and the Android system switcher toggle.

- adb shell am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages
- Scroll past 'Your languages' and 'Your layouts' to the 'Per-app language' and 'System language switcher' groups

> Toggle 'List languages in Android's switcher' on beforehand so the 'Show app name first' sub-toggle is also visible in frame.

### 10. `languages/languages-screen-order`

**Shot:** The Languages screen: Your languages, Add language, and Switch order.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages

> Switch order only appears once more than one layout is enabled (true by default with 4). Capture the top of the screen showing Your languages, Add language, and the Switch order group.

### 11. `languages/system-switcher-toggle`

**Shot:** Languages → System language switcher: register every enabled layout as an Android input-method subtype.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages
- Scroll to the System language switcher group near the bottom of the screen.

> osLanguageSwitcher defaults to true, so 'List languages in Android's switcher' is on and the 'Show app name first' sub-toggle (default off) should be visible beneath it.

### 12. `languages/bengali-language-screen`

**Shot:** Bengali's language screen: layout toggles, numeral system, and dictionary/emoji status.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages
- Tap the "বাংলা · Bangla" row under Your languages to open its detail screen.
- Screenshot showing the Layouts group (Avro/Probhat/Jatiya toggles), scrolled to also include Numerals if possible.

> "language/bn" is not on the deep-link allowlist (only typing, appearance, themes, languages, tools, search are) — must tap through manually from the Languages screen.

### 13. `languages/conjunct-backspace-toggle`

**Shot:** Conjunct-aware backspace, under Languages → Complex scripts. Off by default.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages
- Scroll to the bottom of the Languages screen to the "Complex scripts" group.
- Screenshot the "Conjunct-aware backspace" toggle in its default (off) state.

> This group only renders when a conjunct-forming (INDIC_CLUSTER) script is enabled; Bengali's default-enabled layouts satisfy that.

### 14. `reference/troubleshooting-empty-dictionary-state`

**Shot:** A language with no built-in or downloaded dictionary yet — suggestions come only from what you've typed.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages
- Tap a language that has neither a built-in dictionary nor a downloaded one yet (e.g. French or German — see languages/dictionaries.mdx for which ship built-in) to open its language detail screen
- Scroll to the Dictionary section and capture the 'nothing available yet, download one or import your own' state

> Reuses the same UI as languages/dictionaries.mdx's own dictionary-download-row shot but should show the empty/no-dictionary state specifically, since that's the troubleshooting angle (not the tier picker or an in-progress download).

### 15. `reference/settings/languages-top`

**Shot:** The Languages screen: Your languages, Add language, and (once more than one layout is enabled) Switch order.

- adb shell am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages

> Fresh install shows 2 rows (English, বাংলা) plus Add language; Switch order group is visible since 4 layouts are enabled by default. Capture without scrolling.

### 16. `reference/settings/add-language-screen`

**Shot:** Add language: search matches endonym, English name, id, and locale tag.

- adb shell am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages
- tap 'Add language'

> Leave the search box empty so the shot shows registry order (English, বাংলা, Français, ...) rather than alphabetical — this is the visual proof for the 'not alphabetical' claim in the text.

### 17. `reference/settings/languages-bottom`

**Shot:** Further down the screen: Per-app language, System language switcher, and Complex scripts.

- adb shell am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages
- scroll to the bottom of the screen

> Complex scripts group is visible by default because Bengali (INDIC_CLUSTER script) is enabled out of the box.

### 18. `reference/settings/language-detail-bengali`

**Shot:** Bengali's own screen: Layouts, Numerals, Also suggest from, Dictionary, and Emoji.

- adb shell am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages
- tap 'বাংলা · Bangla'

> Bengali is a good representative because it's enabled by default, has a bundled dictionary, a downloadable wordlist, an emoji pack, and shows Also suggest from (English is the other enabled language) — but has no CJK options group.

### 19. `reference/settings/cjk-options-chinese`

**Shot:** Chinese options: the conversion-dictionary packs, Traditional characters, Fuzzy Pinyin, and Double Pinyin.

- adb shell am start -a android.intent.action.VIEW -d wmkeyboard://settings/languages
- tap 'Add language', search 'chinese', tap the Chinese entry to add and open it
- scroll to the 'Chinese options' group at the bottom

> Capture with Traditional characters toggled on so the Standard/Taiwan/Hong Kong radio group is also visible in frame. Note for the capture operator: a stale code comment in LanguageSettingsScreens.kt implies only the Xiaohe Double Pinyin scheme is selectable ('coming soon' for the rest) — verified false in current source (DoublePinyin.kt has tables for all 5 non-Off schemes), so the screenshot should show none of them disabled.

---

## Settings › Addons  (13 shots)

```bash
python3 docs/screenshots/import.py settings-addons
```

### 1. `addons/repo-catalogue-grid`

**Shot:** A repository's catalogue: search, type filters, and a two-column grid of addon cards.

- am start -a android.intent.action.VIEW -d "wmkeyboard://addons"  (addon-specific deep link handled by AddonDeepLink.kt, separate from the wmkeyboard://settings/<route> allowlist)
- Tap the seeded sample repository row (added automatically on first Addons-screen visit) to open its catalogue
- Optionally select a type filter chip so the grid shows a visually varied set of cards with previews

> Shows AddonRepoScreen (AddonScreens.kt:572+): search field, horizontally-scrolling type chips, 2-column AddonCard grid. Use the app's seeded repo (AddonStore.SEED_URL) so cards have real previews/metadata.

### 2. `addons/add-repository-dialog`

**Shot:** Pasting a repository address. The preview line under the field shows exactly what will be fetched.

- am start -a android.intent.action.VIEW -d "wmkeyboard://addons"
- Tap 'Add repository'
- Type a github.com/user/repo style URL into the field so the 'Will read: <resolved url>' preview line is visible under it

> AddRepositoryDialog (AddonScreens.kt:521-568). Capture with a valid-looking URL so the preview shows the resolved raw.githubusercontent.com address, not the error state.

### 3. `addons/detail-page-licence`

**Shot:** An addon's detail page, with its licence and repository details.

- am start -a android.intent.action.VIEW -d "wmkeyboard://addons"
- Open the seeded sample repository's catalogue
- Tap a card for an addon that declares a licence (the sample manifest's icon packs/fonts/theme typically carry one) to open AddonDetailScreen
- Scroll so the 'Details' group (Repository / Size / Language / Licence rows) is visible

> AddonDetailScreen 'Details' SettingsGroup, LicenseRow at AddonScreens.kt:1468-1528. Pick an entry where entry.hasLicense is true so the Licence row actually appears.

### 4. `addons/apply-prompt`

**Shot:** The prompt after a first install, offering to switch to what you just installed.

- am start -a android.intent.action.VIEW -d "wmkeyboard://addons"
- Open the seeded sample repository's catalogue
- Tap Install on a Theme (or IconPack/EmojiFont/Sound/Layout/Plugin) card that is not yet installed
- Wait for the download to finish; the 'Installed {name}' AlertDialog with the switch-to-it question appears automatically

> AddonApplyPrompt composable (AddonScreens.kt:405-432). Only fires on a genuinely first-time install (no prior record for that key) — use an addon you haven't installed on the capture device yet. A Theme install gives the 'Use it as your keyboard theme now?' / 'Switch to it' wording used in the page's table.

### 5. `addons/update-chip-catalogue`

**Shot:** A repository's catalogue, with an Update chip on a card that has a newer version available.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.MainActivity
- In-app: tap Settings, then tap Addons (not deep-link allowlisted — addons is not in the typing/appearance/themes/languages/tools/search set, so navigate by tap)
- Tap the seeded sample repository (or any added repository) to open its catalogue grid
- Requires fixture state: an addon in that repository's manifest must already be installed locally with sha.version older than the manifest's current entry.version, so its card renders the amber 'Update' AssistChip instead of the check-mark or download icon

> AddonScreens.kt StatusBadge() renders an AssistChip labeled 'Update' in the card's corner (top-right, ~32dp) only for AddonStatus.UpdateAvailable; NotInstalled/Installed/Downloading states look different. Capturing this needs a manifest with two versions of the same addon id, or a locally-edited installed.json record with an older version string, before opening the screen.

### 6. `addons/detail-page-actions`

**Shot:** An addon's detail page: Update, Uninstall, and a button to jump to where it's used.

- adb shell am start -n com.wasimaster.wmkeyboard/.app.MainActivity
- In-app: Settings > Addons > (a repository) > tap an addon that is already installed and has an update available
- Screenshot the detail page's action row: 'Update' button, red-tinted 'Uninstall' button, and (below) the 'Use — open <type>' outlined button

> Same fixture requirement as the catalogue shot — an installed addon whose local version is older than the manifest's version, so both Update and Use buttons are visible together (AddonScreens.kt ~1390-1418).

### 7. `addons/addons-home`

**Shot:** The Addons screen: repositories you've added, plus everything you've installed from them.

- Settings home > Features > Addons (route "addons" is not on the wmkeyboard:// deep-link allowlist, so navigate by tapping: Settings home, Features group, Addons tile)
- Add at least one repository (the seeded sample repo qualifies) and install a couple of addons beforehand so both the Repositories and Installed sections have content

> Screen shows an Add repository / Refresh button row, a Repositories list, and (if anything installed) an Installed list below it. Capture with the seeded sample repository present plus 1-2 installed items so both groups render.

### 8. `addons/plugin-trust-preview`

**Shot:** A plugin's detail page shows what it can do before you install it — this is the one addon type that isn't pure data.

- From the Addons screen, open a repository that lists a plugin, then open that plugin's detail page
- The "What this plugin can do" card sits above the Install button, listing its declared permissions (or "Nothing outside its own panel" if it declares none) plus a caption about the sandbox

> This is the PluginPreview composable in AddonScreens.kt. Use a plugin from the sample repository (wasi-master/wmkeyboard-addon-repository) that declares at least one permission (e.g. local storage) so the list isn't empty/less illustrative.

### 9. `reference/addon-link-prefilled-repo`

**Shot:** Following a wmkeyboard://repo?url=… link pre-fills the add-repository dialog — nothing is fetched until you tap Add.

- adb shell am start -a android.intent.action.VIEW -d "wmkeyboard://repo?url=github.com/wmkeyboard/sample-repo"
- This host is BROWSABLE (declared in the manifest for addons|repo|addon), so a plain implicit VIEW intent works; substitute a real/reachable sample repo URL so AddonRepoCodec.resolveManifestUrl succeeds and the dialog actually opens pre-filled rather than falling back to the plain Addons screen.

> Capture the Addons screen with the add-repository dialog open and the URL field already populated from the link, before tapping Add.

### 10. `reference/settings/addons-home`

**Shot:** The Addons screen: repositories you've added, plus everything installed from them.

- adb shell am start -a android.intent.action.VIEW -d "wmkeyboard://addons"  # this is AddonDeepLink's own scheme, not the wmkeyboard://settings/<route> shortcut allowlist (which does NOT include "addons")

> Best captured once the seeded sample repository (AddonStore.SEED_URL) has finished loading, so both the Repositories and Installed groups have content instead of the empty/loading state.

### 11. `reference/settings/addon-detail-page`

**Shot:** An addon's detail page: previews, description, licence, and the install action.

- adb shell am start -a android.intent.action.VIEW -d "wmkeyboard://addons"
- Tap the seeded sample repository row to open its catalogue
- Tap an addon card that has at least one preview screenshot in its manifest, so the PreviewGallery section renders

> No direct deep link exists to one specific addon without already knowing its repo URL and addon id (that's the wmkeyboard://addon?repo=...&id=... form, documented in reference/deep-links.mdx). Pick whichever card in the sample repo has previews.

### 12. `reference/settings/plugin-detail-screen`

**Shot:** A plugin's own screen: enabled switch, capabilities, stored data, and the log.

- Turn on Allow plugins (Tools › Plugins)
- Install any .wmplugin file (from an addon repository's Plugin entry, or Install from a file with a prepared sample plugin)
- From the Plugins screen's Installed group, tap "Manage <plugin name>"

> Needs a real installed plugin (route plugin/{pluginId}) — use a small sample .wmplugin fixture if the seeded addon repo has no Plugin-type entry. The Log group only appears once the plugin has printed or errored at least once, so trigger it before capturing if that group should be visible.

### 13. `themes/icons-addon-apply-prompt`

**Shot:** After installing an icon pack from an addon repository, the app asks whether to switch to it.

- Requires a test addon repository containing at least one icon_pack entry
- Navigate to Settings > Addons, open that repository, tap Install on an icon pack
- Capture the resulting 'Use its icons on the keyboard now?' dialog (Switch to it / dismiss buttons)

> Needs a seeded test repo URL from whoever owns addon-repo fixtures — flagging as the one shot in this set that isn't reachable with just the shipped app.

---

## Settings › Appearance  (6 shots)

```bash
python3 docs/screenshots/import.py settings-appearance
```

### 1. `reference/settings/appearance-overview`

**Shot:** The Appearance settings screen, showing Style, Keys, and Toolbar.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/appearance

> Top of the Appearance screen — Style group (3 nav rows) and start of Keys group should be visible without scrolling on a typical device.

### 2. `reference/settings/appearance-keys`

**Shot:** The Keys sliders — corner radius, label size, and hint size.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/appearance
- scroll to the Keys group

> Scroll past the Style group's three nav rows to reach the Keys group's three sliders.

### 3. `reference/settings/appearance-toolbar`

**Shot:** The Toolbar group, with the confirmation dialog for turning the toolbar off.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/appearance
- scroll to the Toolbar group
- tap the 'Show the toolbar' toggle to trigger the 'Disable the toolbar?' AlertDialog

> Two options: capture the flat toolbar row list, or capture the AlertDialog overlay (title 'Disable the toolbar?', MainActivity.kt:3401-3423) triggered by turning off 'Show the toolbar'. The dialog is the more distinctive shot since the row list itself is long (13 rows) and won't fit one screen anyway.

### 4. `reference/settings/icons-overview`

**Shot:** The Icons screen: the active pack (with export and delete on each installed row), then the slot list.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/appearance
- Tap the 'Icons' row under Style to open the Icons screen ('icons' is not itself in the shortcut deep-link allowlist, so route there via Appearance)
- Scroll so the Icon pack group (Built-in icons + at least one installed pack with its Export/Delete icon buttons visible) is in frame

> Install at least one icon pack beforehand (import a .wmicons file, or install one from an addon repo) so an installed-pack row with Export/Delete trailing icons is visible, not just Built-in icons.

### 5. `reference/settings/icons-delete-confirm`

**Shot:** Confirming deletion of an installed icon pack.

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/appearance
- Tap 'Icons' to open the Icons screen
- Install an icon pack first if none is present, then tap the trash/Delete icon on that pack's row to open the 'Delete <pack name>?' AlertDialog

> Dialog text is 'Delete <pack name>? Its <N> icon(s) are removed from this device.' with Delete/Cancel buttons.

### 6. `reference/settings/icons-slot-picker`

**Shot:** The per-slot picker: search, the built-in grid, and Use my own SVG…

- am start -a android.intent.action.VIEW -d wmkeyboard://settings/appearance
- Tap 'Icons' to open the Icons screen
- Tap any slot row (e.g. 'Shift' in the Keys group) to open its IconPickerDialog

> Dialog shows a 'Search Material Symbols' field, a scrollable grid of the 181 built-in icons, a 'Use my own SVG…' button, and 'Use default'/'Cancel' buttons.

---
