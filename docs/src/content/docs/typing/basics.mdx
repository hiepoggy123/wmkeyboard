---
title: "Keys & typing basics"
description: "Key presses, long-press popups, shift behaviour, the number row and key previews."
sidebar:
  order: 1
---

:::caution[Draft page]
Scaffolding only — real content pending. The outline below is the checklist for what this page must cover.
:::

## Planned coverage

- Tap, long-press popups (accents, symbols), key preview bubbles
- Shift: single, double-tap caps lock (timing window), auto-capitalisation rules
- Hold-A/C/X/V/Z/Y editing shortcuts (select all, copy, cut, paste, undo, redo)
- Enter key: action icons (search/send/go/next/done) and behaviour options
- Number row on/off; numeral systems (native digits per language)
- Smart key-hit detection: hitboxes lean toward the likely next letter
- Multi-touch typing; key repeat

{/* TODO(sonnet): replace this stub. Turn the outline into real sections,
   add screenshots with <PhoneFrame>, mark settings with <SettingsPath>,
   keys with <KeyCap>, and edition limits with <Flavor>.
   Conventions live in docs/CONTENT_GUIDE.md. */}
