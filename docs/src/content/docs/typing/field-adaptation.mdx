---
title: "The keyboard adapts to the field"
description: "Email fields get @, URL fields get /, password fields get privacy — automatically."
sidebar:
  order: 4
---

:::caution[Draft page]
Scaffolding only — real content pending. The outline below is the checklist for what this page must cover.
:::

## Planned coverage

- FieldKind detection: email, URI, number, phone, password, date/time
- The email/URL extra keys; dedicated keypads
- Password & secure fields: suggestions off, learning off, clipboard rules
- When an app lies about its field type — overrides and workarounds

{/* TODO(sonnet): replace this stub. Turn the outline into real sections,
   add screenshots with <PhoneFrame>, mark settings with <SettingsPath>,
   keys with <KeyCap>, and edition limits with <Flavor>.
   Conventions live in docs/CONTENT_GUIDE.md. */}
