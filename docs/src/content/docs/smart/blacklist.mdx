---
title: "Suggestion blacklist"
description: "Words the keyboard must never suggest, enforced across every suggestion path."
sidebar:
  order: 4
---

:::caution[Draft page]
Scaffolding only — real content pending. The outline below is the checklist for what this page must cover.
:::

## Planned coverage

- Adding via long-press on a suggestion; the editor screen
- Blacklist applies to completions, predictions, corrections — everywhere
- Removing entries; interaction with learned words

{/* TODO(sonnet): replace this stub. Turn the outline into real sections,
   add screenshots with <PhoneFrame>, mark settings with <SettingsPath>,
   keys with <KeyCap>, and edition limits with <Flavor>.
   Conventions live in docs/CONTENT_GUIDE.md. */}
