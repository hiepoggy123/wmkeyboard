---
title: "Password manager & autofill chips"
description: "Your password manager's suggestions, shown right in the suggestion strip."
sidebar:
  order: 9
---

:::caution[Draft page]
Scaffolding only — real content pending. The outline below is the checklist for what this page must cover.
:::

## Planned coverage

- Inline autofill: credential chips from your password manager (Android 11+)
- Which managers work (any autofill service that supports inline suggestions)
- Tapping a chip fills the field — the keyboard never sees the secret
- Turning it off; interaction with incognito and secure fields

{/* TODO(sonnet): replace this stub. Turn the outline into real sections,
   add screenshots with <PhoneFrame>, mark settings with <SettingsPath>,
   keys with <KeyCap>, and edition limits with <Flavor>.
   Conventions live in docs/CONTENT_GUIDE.md. */}
