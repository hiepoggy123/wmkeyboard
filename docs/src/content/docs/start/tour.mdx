---
title: "A two-minute tour"
description: "The suggestion strip, toolbar, emoji, tools and settings — where everything lives."
sidebar:
  order: 3
---

import PhoneFrame from '@components/PhoneFrame.astro';
import SettingsPath from '@components/SettingsPath.astro';
import KeyCap from '@components/KeyCap.astro';
import Flavor from '@components/Flavor.astro';

:::caution[Draft page]
Scaffolding only — real content pending. The outline below is the checklist for what this page must cover.
:::

## Component showcase

This section demos the docs components and is deleted once real content lands.

Long-press <KeyCap>?123</KeyCap>, tap <KeyCap>⌫</KeyCap>, or hold <KeyCap>Space</KeyCap> to switch languages.

<SettingsPath path="Typing / Gestures / Spacebar swipes" />

Offline Whisper dictation is <Flavor edition="full" /> while everything on this page is <Flavor edition="both" />.

<PhoneFrame caption="An empty PhoneFrame renders this placeholder until a screenshot exists." />

## Planned coverage

- Anatomy screenshot: strip, toolbar, key area, symbol row
- The toolbar and where tools open (panels replace the key area)
- Long-press = more: keys, suggestions, toolbar icons, emoji
- Opening settings from the keyboard
- The settings app map (one screen per section, link to Reference → Settings)

{/* TODO(sonnet): replace this stub. Turn the outline into real sections,
   add screenshots with <PhoneFrame>, mark settings with <SettingsPath>,
   keys with <KeyCap>, and edition limits with <Flavor>.
   Conventions live in docs/CONTENT_GUIDE.md. */}
