---
title: "File formats"
description: "Every file WM Keyboard reads or writes: extensions, structure, and links to full specs."
sidebar:
  order: 4
---

:::caution[Draft page]
Scaffolding only — real content pending. The outline below is the checklist for what this page must cover.
:::

## Planned coverage

- .wmdict binary dictionaries (mmap format)
- .wmplugin Lua plugin packages
- .wmstickers sticker packs
- Theme export files; addon repo manifest (wmkeyboard-repo.json + JSON schema link)
- Where exports land on disk

{/* TODO(sonnet): replace this stub. Turn the outline into real sections,
   add screenshots with <PhoneFrame>, mark settings with <SettingsPath>,
   keys with <KeyCap>, and edition limits with <Flavor>.
   Conventions live in docs/CONTENT_GUIDE.md. */}
