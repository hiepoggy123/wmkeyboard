---
title: "Snippets & text expansion"
description: "Reusable text blocks with variables, inserted from a panel or expanded by keyword."
sidebar:
  order: 13
---

:::caution[Draft page]
Scaffolding only — real content pending. The outline below is the checklist for what this page must cover.
:::

## Planned coverage

- The snippets panel: browse and insert saved blocks
- Trigger keywords: type a shortcut, get the expansion
- Variables — all 18: `{date}`, `{time}`, `{time12}`, `{datetime}`, `{isodate}`, `{isotime}`, `{weekday}`, `{day}`, `{month}`, `{year}`, `{timezone}`, `{timestamp}`, `{clip}`, `{selection}`, `{app}`, `{package}`, `{uuid}`, `{cursor}` — plus parametrised `{date:pattern}`
- Managing snippets in settings (create, edit, reorder)
- Snippet packs as addons (.wmsnippets.json import/export)

{/* TODO(sonnet): replace this stub. Turn the outline into real sections,
   add screenshots with <PhoneFrame>, mark settings with <SettingsPath>,
   keys with <KeyCap>, and edition limits with <Flavor>.
   Conventions live in docs/CONTENT_GUIDE.md. */}
