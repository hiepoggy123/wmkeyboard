---
title: "The toolbar"
description: "One row, many tools: how the toolbar works and how to make it yours."
sidebar:
  label: "Overview"
  order: 1
---

:::caution[Draft page]
Scaffolding only — real content pending. The outline below is the checklist for what this page must cover.
:::

## Planned coverage

- The toolbar and the toolbox: pinned vs full list; reordering
- All 60 tools at a glance — grouped index with links (panels, keyboard controls, knowledge, scanners, utilities, cursor tools)
- Each tool has its own settings screen under Settings → Tools
- Panels capture keys — what that means and how to escape
- Which tools still work on the lock screen (direct-boot-safe set)
- Hardware leader key access (double-tap Ctrl + letter)

{/* TODO(sonnet): replace this stub. Turn the outline into real sections,
   add screenshots with <PhoneFrame>, mark settings with <SettingsPath>,
   keys with <KeyCap>, and edition limits with <Flavor>.
   Conventions live in docs/CONTENT_GUIDE.md. */}
