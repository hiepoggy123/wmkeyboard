---
title: "Testing"
description: "Unit tests on the JVM, Compose UI tests, and where tests live."
sidebar:
  order: 3
---

:::caution[Draft page]
Scaffolding only — real content pending. The outline below is the checklist for what this page must cover.
:::

## Planned coverage

- Running: ./gradlew testFullDebugUnitTest (unprefixed task gotcha)
- Where tests live (mostly :app) and why
- Compose UI tests for the keyboard view
- JVM-testable core: engines have no Android deps
- Device smoke-testing checklist

{/* TODO(sonnet): replace this stub. Turn the outline into real sections,
   add screenshots with <PhoneFrame>, mark settings with <SettingsPath>,
   keys with <KeyCap>, and edition limits with <Flavor>.
   Conventions live in docs/CONTENT_GUIDE.md. */}
