---
title: "Building from source"
description: "Clone, build, install — JDK, SDK and flavor details."
sidebar:
  order: 2
---

:::caution[Draft page]
Scaffolding only — real content pending. The outline below is the checklist for what this page must cover.
:::

## Planned coverage

- Prerequisites: JDK 17+ (21 toolchain auto-provisioned), Android SDK 36
- assembleFullDebug vs assembleLiteDebug; what flavors change
- Native bits: the Harper Rust JNI build (cargo-ndk)
- Running static analysis (./gradlew staticAnalysis)
- Common build problems

{/* TODO(sonnet): replace this stub. Turn the outline into real sections,
   add screenshots with <PhoneFrame>, mark settings with <SettingsPath>,
   keys with <KeyCap>, and edition limits with <Flavor>.
   Conventions live in docs/CONTENT_GUIDE.md. */}
