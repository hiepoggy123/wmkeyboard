---
title: "Network policy"
description: "Every feature that can touch the network, what it sends, and how to keep it off."
sidebar:
  order: 4
---

:::caution[Draft page]
Scaffolding only — real content pending. The outline below is the checklist for what this page must cover.
:::

## Planned coverage

- The complete table: tool → endpoint → payload → when
- API keys: bring-your-own model; where keys are stored
- Downloads (dictionaries, models, addons): sources and integrity
- Blocking it all: lite habits, per-tool disable, no-network guarantee for typing

{/* TODO(sonnet): replace this stub. Turn the outline into real sections,
   add screenshots with <PhoneFrame>, mark settings with <SettingsPath>,
   keys with <KeyCap>, and edition limits with <Flavor>.
   Conventions live in docs/CONTENT_GUIDE.md. */}
