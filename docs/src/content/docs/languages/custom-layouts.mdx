---
title: "Custom layouts & the layout editor"
description: "Edit any keyboard layout visually, or as raw JSON — and share the result."
sidebar:
  order: 8
---

:::caution[Draft page]
Scaffolding only — real content pending. The outline below is the checklist for what this page must cover.
:::

## Planned coverage

- The visual keymap editor: remapping keys, long-press popups, rows
- The raw JSON editor for power users
- Layout file format (.wmlayout.json) — export, import, file association
- Installing community layouts from addon repositories
- 375 bundled asset layouts + 18 built-ins: how shipping layouts relate to custom ones

{/* TODO(sonnet): replace this stub. Turn the outline into real sections,
   add screenshots with <PhoneFrame>, mark settings with <SettingsPath>,
   keys with <KeyCap>, and edition limits with <Flavor>.
   Conventions live in docs/CONTENT_GUIDE.md. */}
